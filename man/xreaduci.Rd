% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-uci.R
\name{xreaduci}
\alias{xreaduci}
\title{Read a .uci dataset}
\usage{
xreaduci(file, directed = NULL, mode = NULL, title = NULL, ...)
}
\arguments{
\item{file}{Path to the \code{.uci} file.}

\item{directed, mode, title}{Override what the file says. \code{title} defaults to
the file's own title, or the file name when it has none.}

\item{...}{Reserved.}
}
\value{
An \code{xucinet} object. When the file carries node attributes they are
attached as \verb{$attributes}, a plain data frame keyed by node label; it is
\code{NULL} otherwise and no routine reads it (SPEC D1).
}
\description{
Reads the single-file JSON format described in
\code{system.file("schema/uci-1.0.json", package = "xucinet")}: matrices, labels,
mode, directedness, relation names and, if the file carries them, node
attributes.
}
\examples{
f <- system.file("schema", "campnet-example.uci", package = "xucinet")
xreaduci(f)
}
\seealso{
\code{\link[=xsaveuci]{xsaveuci()}}, and \code{\link[=xreaducinet]{xreaducinet()}} for the older \verb{##h}/\verb{##d} pair.
}
