% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xdensity.R
\name{xdensity}
\alias{xdensity}
\title{Density and average degree}
\usage{
xdensity(net, directed = NULL, weighted = NULL, diagonal = FALSE)
}
\arguments{
\item{net}{A network (any accepted form).}

\item{directed}{\code{NULL} (detect), \code{TRUE} or \code{FALSE}. Detection is reported as
an assumption in the printed output.}

\item{weighted}{\code{NULL} (use values if the matrix is valued), \code{TRUE} or
\code{FALSE} (dichotomize at > 0 first).}

\item{diagonal}{Logical; include the diagonal? UCINET's default is \code{FALSE}.}
}
\value{
An object of class \code{c("xdensity", "xucinet_output")} with \verb{$summary}
holding density, average degree, standard deviation of ties, and number
of ties; one section per relation for multi-relation data.
}
\description{
UCINET: Network | Whole Networks | Density | Density Overall. Phase 0 pilot
routine: the first function golden-tested against UCINET output, and the
template every later routine follows.
}
\examples{
m <- matrix(c(0,1,1, 1,0,0, 1,0,0), 3, 3)
xdensity(m)
}
