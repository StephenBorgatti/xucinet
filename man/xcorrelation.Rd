% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xregression.R
\name{xcorrelation}
\alias{xcorrelation}
\title{Node-level correlation with a permutation test}
\usage{
xcorrelation(x, y, data = NULL, nperm = 10000, seed = NULL, tails = 2)
}
\arguments{
\item{x, y}{Two numeric vectors, one value per node, or names of columns of
\code{data}.}

\item{data}{Optional: a data frame, node-level result or network with
attributes in which \code{x} and \code{y} are looked up.}

\item{nperm}{Number of random permutations, 10000 by default.}

\item{seed}{Random seed.}

\item{tails}{2 (the default) or 1.}
}
\value{
An object of class \code{c("xcorrelation", "xucinet_output")} whose
\verb{$summary} holds the correlation, the number of cases, the classical and
permutation significance and the three proportions.
}
\description{
The correlation between two node attributes, with a p-value from permuting
one of them across the nodes (book, 14.4). UCINET has no separate routine:
this is its Node-level Regression with one predictor, whose standardized
coefficient is the correlation.
}
\examples{
xcorrelation(c(1, 3, 2, 5, 4, 6), c(1, 2, 3, 4, 5, 7), nperm = 1000, seed = 1)
}
\seealso{
\code{\link[=xregression]{xregression()}}, \code{\link[=xqap]{xqap()}} for two networks.
}
