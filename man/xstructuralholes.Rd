% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xstructuralholes.R
\name{xstructuralholes}
\alias{xstructuralholes}
\title{Structural holes}
\usage{
xstructuralholes(
  net,
  relation = NULL,
  method = c("ego", "whole"),
  direction = c("undirected", "out", "in", "reciprocated"),
  symmetrize = TRUE,
  diagonal = FALSE,
  isolate = c(0, NA),
  pendant = c(1, NA)
)
}
\arguments{
\item{net}{A network (any accepted form). 1-mode.}

\item{relation}{Which relation of a multi-relation dataset, by name or
position. Defaults to the first.}

\item{method}{\code{"ego"} (the default, UCINET's ego network model) or
\code{"whole"} (the whole network model).}

\item{direction}{Who counts as an alter in the ego-network model:
\code{"undirected"} (the default, UCINET's \emph{Union}), \code{"out"}, \code{"in"} or
\code{"reciprocated"} (\emph{Intersection}). The whole-network model uses it only
for the ego-network columns.}

\item{symmetrize}{Add \code{z(i,j)} and \code{z(j,i)} before computing proportions,
as Burt does? \code{TRUE} by default.}

\item{diagonal}{Keep the diagonal? \code{FALSE} by default.}

\item{isolate, pendant}{What to report for an ego with no alters, and for one
with a single alter, in the ego-network model: a pair of values, effective
size then constraint. UCINET's defaults are \code{c(0, NA)} and \code{c(1, NA)}.}
}
\value{
An object of class \code{c("xstructuralholes", "xucinet_output")}.
\verb{$nodes} holds the measures in original node order under UCINET's
headings; \verb{$matrices} holds \verb{Dyadic Redundancy} and \verb{Dyadic Constraint},
ego by alter.
}
\description{
UCINET: Network | Ego Networks | Structural Holes. Burt's measures of how
far a node's contacts are redundant with one another: effective size,
efficiency, constraint and hierarchy, with the dyadic redundancy and
dyadic constraint matrices they are built from.
}
\details{
Tie values are used as they are. With \code{symmetrize = TRUE} (the default, "ala
Burt") the strength of the relation between \code{i} and \code{j} is \code{z(i,j) + z(j,i)};
\code{p(i,j)} is that strength as a share of \code{i}'s total, and \code{m(i,j)} as a share
of \code{i}'s strongest.
\itemize{
\item \strong{Effective size} is the number of alters less their redundancy:
the sum over alters \code{j} of \verb{1 - sum_q p(i,q) m(j,q)}.
\item \strong{Efficiency} is effective size over the number of alters.
\item \strong{Constraint} sums, over alters \code{j}, \verb{(p(i,j) + sum_q p(i,q) p(q,j))^2},
the dyadic constraint.
\item \strong{Hierarchy} is how unevenly constraint is spread across the alters
(Burt's Coleman-Theil index), 1 for an ego with a single alter.
}

Two models, as in the dialog. The \strong{ego-network model} (the default)
computes everything inside each node's ego network, so ties from an alter to
someone outside it have no effect; it also reports ego betweenness,
log constraint, indirect constraint and the alters' density. The
\strong{whole-network model} computes the proportions over the whole network, as
Burt's formulas are usually written, and prints the first four measures
plus indirect constraint, as UCINET does. Both models return the same eleven
columns: under the whole model, Degree and Ln(Constraint) follow its own
counts, and ego betweenness, density, average degree and open pairs, which
describe the ego network itself, are the same as under the ego model.
}
\section{UCINET equivalent}{

Network | Ego Networks | Structural Holes. \emph{Method} is \code{method}, \emph{How to
define ego net} is \code{direction}, \emph{Symmetrize by sum ala Burt} is
\code{symmetrize}, \emph{Diagonal valid} is \code{diagonal}, and the \emph{Set isolates to} and
\emph{Set pendants to} boxes are \code{isolate} and \code{pendant}.
}

\examples{
xstructuralholes(campnet)
xstructuralholes(campnet, method = "whole")
}
\seealso{
\code{\link[=xegonet]{xegonet()}}.
}
