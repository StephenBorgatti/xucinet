% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xreach.R
\name{xbetareach}
\alias{xbetareach}
\title{Beta reach centrality}
\usage{
xbetareach(
  net,
  relation = NULL,
  beta = 0.8,
  directed = NULL,
  direction = c("out", "in")
)
}
\arguments{
\item{net}{A network (any accepted form).}

\item{relation}{Which relation of a multi-relation dataset, by name or
position. Defaults to the first.}

\item{beta}{The distance discount, between 0 and 1. UCINET's default is 0.8.}

\item{directed}{\code{NULL} (detect from symmetry), \code{TRUE} or \code{FALSE}.}

\item{direction}{\code{"out"} (default) or \code{"in"}.}
}
\value{
An \code{xucinet_output} whose \verb{$nodes} has one \verb{Beta Reach} column.
}
\description{
UCINET: Network | Centrality | Beta Reach Centrality. Reach with distance
discounted rather than ignored: a node two steps away counts \code{beta} as much
as one a single step away, three steps \code{beta} squared, and so on.
}
\details{
The score is \code{sum(beta^(d - 1)) / (n - 1)} over the nodes a node can reach.
At \code{beta = 1} it is the plain proportion reachable; at \code{beta = 0} it is
degree over \code{n - 1}. UCINET's dialog defaults to 0.8 and so does this.
}
\examples{
xbetareach(campnet)
}
