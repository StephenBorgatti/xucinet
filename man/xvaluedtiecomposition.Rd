% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xtiecomposition.R
\name{xvaluedtiecomposition}
\alias{xvaluedtiecomposition}
\title{Egonet valued tie composition}
\usage{
xvaluedtiecomposition(
  net,
  relation = NULL,
  direction = c("out", "in", "both", "reciprocated", "equal"),
  op = c("!=", ">", ">=", "==", "<=", "<"),
  cutoff = 0,
  diagonal = FALSE
)
}
\arguments{
\item{net}{A network (any accepted form), usually with several relations.}

\item{relation}{Which relation of a multi-relation dataset, by name or
position. Defaults to the first.}

\item{direction}{Which of ego's ties count: \code{"out"} (the default), \code{"in"},
\code{"both"} (out and in, a reciprocated pair contributing both values),
\code{"reciprocated"} (both values of each reciprocated pair) or \code{"equal"}
(reciprocated with the same value both ways).}

\item{op, cutoff}{What counts as a tie: the value compared with \code{cutoff} by
\code{op}, one of \code{"!="} (the default), \code{">"}, \code{">="}, \code{"=="}, \code{"<="}, \code{"<"}.}

\item{diagonal}{Count ties to self? \code{FALSE} by default.}
}
\value{
An object of class \code{c("xvaluedtiecomposition", "xucinet_output")}
with the seven statistics in \verb{$nodes}, in original node order.
}
\description{
UCINET: Network | Ego Networks | Egonet Valued Tie Composition. For each
ego, the values of its ties: how many there are, their sum, mean, standard
deviation, minimum, maximum and range.
}
\details{
The standard deviation divides by the number of ties, not one less, as all
UCINET's descriptive statistics do. An ego with no ties has a count of 0 and
everything else missing.
}
\section{UCINET equivalent}{

Network | Ego Networks | Egonet Valued Tie Composition. \emph{Which ties matter?}
is \code{direction}; the valid-ties operator and value are \code{op} and \code{cutoff};
\emph{Include ties to self} is \code{diagonal}.
}

\examples{
xvaluedtiecomposition(camp92)
xvaluedtiecomposition(camp92, direction = "in")
}
\seealso{
\code{\link[=xtiecomposition]{xtiecomposition()}}.
}
