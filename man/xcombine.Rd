% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relations.R
\name{xcombine}
\alias{xcombine}
\title{Collapse the relations of a dataset into one matrix}
\usage{
xcombine(
  net,
  relations = NULL,
  method = c("sum", "mean", "min", "max", "sd", "product"),
  diagonal = FALSE
)
}
\arguments{
\item{net}{A multi-relation network, or a list of separate networks with
matching labels.}

\item{relations}{Which relations to use, by name or position. \code{NULL}, the
default, uses all of them.}

\item{method}{\code{"sum"} (the dialog's default), \code{"mean"}, \code{"min"}, \code{"max"},
\code{"sd"} (the population form) or \code{"product"} (UCINET's elementwise
multiplication).}

\item{diagonal}{Include the diagonal? \code{FALSE} by default, as the dialog's
"diagonal valid" is; when \code{FALSE} the diagonal of a square result comes
back missing rather than summarised.}
}
\value{
A single-relation \code{xucinet} object.
}
\description{
UCINET: Transform | Matrix Operations | Within dataset | Aggregations, and
the between-datasets form of the same thing.
}
\examples{
as.matrix(xcombine(hightech))[1:4, 1:4]
as.matrix(xcombine(hightech, method = "max"))[1:4, 1:4]
}
\seealso{
\code{\link[=xjoin]{xjoin()}}, \code{\link[=xunpack]{xunpack()}}, \code{\link[=xmultiplex]{xmultiplex()}}.
}
