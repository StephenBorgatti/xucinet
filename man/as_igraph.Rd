% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-xucinet.R
\name{as_igraph}
\alias{as_igraph}
\alias{as_network}
\alias{as_tbl_graph}
\title{Convert a network to igraph, statnet or tidygraph form}
\usage{
as_igraph(net, relation = NULL)

as_network(net, relation = NULL)

as_tbl_graph(net, relation = NULL)
}
\arguments{
\item{net}{A network (any accepted form).}

\item{relation}{Which relation of a multi-relation dataset, by name or
position. Defaults to the first.}
}
\value{
An \code{igraph}, \code{network} or \code{tbl_graph} object.
}
\description{
The way out of xucinet and into the rest of the R network ecosystem. Node
labels, directedness, edge values and 2-mode structure are carried across.
A multi-relation dataset has no single-graph equivalent, so pick one with
\code{relation}; the first is used by default.
}
\details{
These also work through the ecosystem's own generics, so \code{igraph::as.igraph(net)},
\code{network::as.network(net)} and \code{tidygraph::as_tbl_graph(net)} all understand
an \code{xucinet} object.
}
\examples{
m <- matrix(c(0,1,1, 1,0,0, 1,0,0), 3, 3,
            dimnames = list(c("a","b","c"), c("a","b","c")))
if (requireNamespace("igraph", quietly = TRUE)) {
  g <- as_igraph(m)
  as_xucinet(g)
}
}
