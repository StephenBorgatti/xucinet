% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xegoaltersimilarity.R
\name{xegoaltersimilarity}
\alias{xegoaltersimilarity}
\title{Ego-alter similarity}
\usage{
xegoaltersimilarity(
  net,
  attribute,
  type = NULL,
  relation = NULL,
  ties = c("out", "any", "in", "reciprocated"),
  method = "negabsdiff",
  normalize = c("none", "additive", "ratio", "interval"),
  data = NULL
)
}
\arguments{
\item{net}{A network (any accepted form). 1-mode.}

\item{attribute}{The attribute: a vector, or the name of a column looked up
first in \code{net}'s attribute table and then in \code{data}.}

\item{type}{\code{"categorical"}, \code{"continuous"}, or \code{NULL} (the default) to
decide from the attribute.}

\item{relation}{Which relation of a multi-relation dataset, by name or
position. Defaults to the first.}

\item{ties}{Which ties are ego's: \code{"out"} (the default, as in UCINET),
\code{"any"} (a tie in either direction), \code{"in"} or \code{"reciprocated"}.}

\item{method}{Continuous only: which measures to print, one or more of
\code{"negabsdiff"} (the default), \code{"zegers"}, \code{"minovermax"}, \code{"absdiff"},
\code{"sqdiff"}, \code{"product"}. All six are always computed.}

\item{normalize}{Continuous only: rescale the attribute first? \code{"none"} (the
default), \code{"additive"} (subtract the mean), \code{"ratio"} (divide by the root
of the sum of squared deviations) or \code{"interval"} (z-scores).}

\item{data}{A data frame to look \code{attribute} up in.}
}
\value{
An object of class \code{c("xegoaltersimilarity", "xucinet_output")}
with the measures in \verb{$nodes}, in original node order.
}
\description{
UCINET: Network | Ego Networks | Ego-Alter Similarity, both the Categorical
and the Continuous item, as one function. For each ego, whether it is tied
to others who resemble it on an attribute: homophily at the level of the
individual.
}
\details{
\code{type} decides which, as in \code{\link[=xaltercomposition]{xaltercomposition()}}: character, factor and
logical attributes are categorical, numbers continuous, and a numeric
attribute that looks like category codes is noted but still treated as
continuous unless \code{type = "categorical"}.

\strong{Categorical.} Every other node is either tied to ego or not, and either
in ego's category or not, which gives a two-by-two table per ego: \code{a} tied
and same, \code{b} tied and different, \code{c} untied and same, \code{d} untied and
different. The network is dichotomized at > 0. The columns are \code{H} (the
proportion of ego's ties that are in its own group, \code{a/(a+b)}), \verb{H*} (H
less the proportion of all others in ego's group), \code{Coleman} (H* rescaled
by its maximum), \code{EI} (\code{(b-a)/(a+b)}), \code{Jaccard}, \verb{Yules Q}, \code{Kappa},
\code{Phi}, \code{Bona} (Bonacich's 1972 measure), \code{Odds_Ratio}, \code{Log_Odds},
\code{fInGroup} (\code{a}) and \code{fOutGroup} (\code{b}), then ego's own value. An ego with no
ties has every measure missing.

\strong{Continuous.} For each ego, the correlation between its row of tie values
and the similarity of ego to each other node on the attribute. One column for
each of six measures, all always computed; \code{method} chooses which are
printed: \code{"negabsdiff"} (the dialog's default, minus the
absolute difference, so that higher is more similar), \code{"zegers"}
(\verb{2xy/(x^2+y^2)}), \code{"minovermax"}, \code{"absdiff"}, \code{"sqdiff"} and \code{"product"}.
The two difference measures are reverse measures: a positive value means
ego is tied to people unlike itself.
}
\section{UCINET equivalent}{

Network | Ego Networks | Ego-Alter Similarity | Categorical, and ... |
Continuous. \emph{Definition of Ego Network} is \code{ties}; the continuous
dialog's \emph{Measures} checklist is \code{method} and \emph{Attribute Normalization} is
\code{normalize}.
}

\examples{
xegoaltersimilarity(campnet, camp92_attr$Gender, type = "categorical")
xegoaltersimilarity(hightech, "Age", data = hightech_attr)
}
\seealso{
\code{\link[=xhomophily]{xhomophily()}} for the whole-network measures, and
\code{\link[=xaltercomposition]{xaltercomposition()}}.
}
