% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xcohesion.R
\name{xcohesion}
\alias{xcohesion}
\title{Whole-network measures}
\usage{
xcohesion(net, directed = NULL)
}
\arguments{
\item{net}{A network (any accepted form). 1-mode only.}

\item{directed}{\code{NULL} (detect), \code{TRUE}, or \code{FALSE} to ignore the direction
of ties, which is the dialog's "Ignore direction of ties" box. UCINET
leaves it unchecked, so \code{NULL} matches the default.}
}
\value{
An object of class \code{c("xcohesion", "xucinet_output")}. \verb{$summary} is
a data frame of the 33 measures, one row each, with one column per
relation - the shape UCINET prints and saves.
}
\description{
UCINET: Network | Whole-Network Measures | Multiple Whole-Network Measures.
Thirty-three measures of the network as a whole, in UCINET's order and under
UCINET's headings.
}
\details{
Valued data are dichotomized at \verb{> 0} and self-ties are removed, both as the
routine does. The K-core index and \verb{Deg Centralization} are always computed
on the underlying undirected graph, whatever \code{directed} says, because
\code{getcohesion} symmetrizes before computing them.

\code{Components} counts \strong{strongly} connected components: the Delphi uses
Tarjan's algorithm. \code{Connectedness} is a different idea - the proportion of
ordered pairs that can reach each other at all - so the two need not agree
about whether a network is in one piece.

\verb{Small Worldness} is reported only when the network is strongly connected,
and the distance measures of a disconnected network are computed over the
reachable pairs only. UCINET prints the same two caveats.
}
\examples{
xcohesion(campnet)

# ignoring direction changes most of the block
xcohesion(campnet, directed = FALSE)
}
\seealso{
\code{\link[=xdensity]{xdensity()}} for the density block alone, \code{\link[=xcomponents]{xcomponents()}} for the
membership rather than the count, and \code{\link[=xgeodesic]{xgeodesic()}} for the distances the
distance measures are built on.
}
