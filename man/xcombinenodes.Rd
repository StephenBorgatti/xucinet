% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xcombinenodes.R
\name{xcombinenodes}
\alias{xcombinenodes}
\title{Aggregate a network by a node attribute}
\usage{
xcombinenodes(
  net,
  attribute,
  method = c("mean", "sum", "count", "max", "min", "sd"),
  diagonal = FALSE,
  data = NULL
)
}
\arguments{
\item{net}{A network (any accepted form). 1-mode only; see the note below.}

\item{attribute}{The partition: a vector (named, or in node order), or the
name of a column, looked up first in \code{net}'s attribute table and then in
\code{data}. One group per distinct value.}

\item{method}{The summary: \code{"mean"} (the dialog's default, and what UCINET
calls the density), \code{"sum"}, \code{"count"} (how many ties are above zero),
\code{"max"}, \code{"min"} or \code{"sd"} (the population form).}

\item{diagonal}{Include the self-ties? \code{FALSE} by default, as the dialog's
"utilize diagonal (reflexive ties)" checkbox is. Forced on for 2-mode
data, where the diagonal is an ordinary cell.}

\item{data}{A data frame to look \code{attribute} up in.}
}
\value{
An \code{xucinet} object with one row and column per group, labelled by
the attribute's values and titled \verb{<name>-blk}. A group pair with no
usable cells comes back missing.
}
\description{
UCINET: Transform | Aggregate | Block - Aggregate by Partitions. Collapses
the network to one row and column per group, holding a summary of the ties
between each pair of groups.
}
\details{
Self-ties are left out unless \code{diagonal = TRUE}. That is the \emph{matrix}
diagonal, not the block diagonal: the within-group cells are all still
there, it is only cell \verb{(i,i)} that goes. Missing cells take no part in the
summary.
}
\examples{
gender <- camp92_attr$Gender
names(gender) <- rownames(camp92_attr)
as.matrix(xcombinenodes(campnet, gender))
as.matrix(xcombinenodes(campnet, gender, method = "sum"))
}
\seealso{
\code{\link[=xattributetomatrix]{xattributetomatrix()}}, which goes the other way.
}
