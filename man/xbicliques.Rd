% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xbicliques.R
\name{xbicliques}
\alias{xbicliques}
\title{Bicliques}
\usage{
xbicliques(net, min_rows = 3, min_cols = 3, relation = NULL)
}
\arguments{
\item{net}{A 2-mode network (any accepted form). Values above 0 are ties.}

\item{min_rows, min_cols}{The fewest rows and columns a biclique may have.
UCINET's defaults are 3 and 3.}

\item{relation}{Which relation, by name or position. Defaults to the first.}
}
\value{
An object of class \code{c("xbicliques", "xucinet_output")}.
\verb{$bicliques} lists each biclique's \code{rows} and \code{cols}. \verb{$matrices} holds,
for each mode, the participation matrix (the share of the biclique's
other-mode members each node is tied to, 1 for members) and the
co-membership matrix (the number of bicliques two nodes share; the
diagonal is the number each is in), and the co-membership of all the
nodes together, which is clustered by average link (the book's Figure
13.4); \verb{$clustering} holds that clustering.
}
\description{
A biclique of a 2-mode network is a set of rows and a set of columns in
which every row is tied to every column, and to which no row or column can
be added: the 2-mode counterpart of a clique. In the Davis data, a set of
women who all attended the same set of events.
}
\details{
UCINET finds them with the command-language function \code{biclique()}; there is
no menu item. The result is shaped like \code{\link[=xcliques]{xcliques()}}': the bicliques in the
order UCINET finds them, a participation matrix for each mode, and the
co-membership of each mode.
}
\section{UCINET equivalent}{

The command \verb{bc = biclique(davis 3 3)}.
}

\examples{
xbicliques(davis)
}
\seealso{
\code{\link[=xcliques]{xcliques()}}, \code{\link[=xbipartite]{xbipartite()}}.
}
