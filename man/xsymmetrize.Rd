% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform.R
\name{xsymmetrize}
\alias{xsymmetrize}
\title{Symmetrize a directed network}
\usage{
xsymmetrize(
  net,
  method = c("max", "min", "mean", "sum", "difference", "product", "division", "lower",
    "upper", "gt", "ge", "eq", "le", "lt", "ne", "absdiffsum"),
  missing = c("nonmissing", "both")
)
}
\arguments{
\item{net}{A network (any accepted form). 1-mode only.}

\item{method}{One of the sixteen the dialog offers, in its order. The
arithmetic ones: \code{"max"} (the default, UCINET's Maximum), \code{"min"},
\code{"mean"}, \code{"sum"}, \code{"difference"} (the \emph{absolute} difference, as in
\code{Arith1}), \code{"product"}, \code{"division"} (upper divided by lower; a zero
lower cell gives \code{NA}), \code{"lower"} (keep the lower half), \code{"upper"} (keep
the upper half) and \code{"absdiffsum"} (\code{abs(a-b)/(a+b)}, \code{NA} when both are
zero). The comparisons, which give 1 or 0: \code{"gt"}, \code{"ge"}, \code{"eq"},
\code{"le"}, \code{"lt"} and \code{"ne"}, each reading "upper \emph{op} lower".}

\item{missing}{\code{"nonmissing"} (the dialog's default) takes the observed
value when only one cell of the pair is missing; \code{"both"} makes the pair
missing whenever either cell is. UCINET stores missing as a large
sentinel and applies \code{min()} or \code{max()} to the raw cells, which is what
these two amount to.}
}
\value{
An \code{xucinet} object with \code{directed = FALSE}, titled the way UCINET
names it for the method chosen (\code{campnet-maxsym}, \code{campnet-avgsym}, ...).
The \code{history} attribute carries the six figures UCINET's log prints: the
density before, the number and percentage of symmetric pairs, the number
and percentage of reciprocated dyads, the density after, and the
correlation with the input.
}
\description{
UCINET: Transform | Symmetrize. Replaces each pair of cells \code{x[i,j]} and
\code{x[j,i]} with a single value computed from both, so the result is
undirected.
}
\details{
The diagonal is left exactly as it was: UCINET's loop runs over the strict
upper triangle (\verb{for i := 1 to nr-1, for j := i+1 to nc}), so \code{x[i,i]}
never enters the calculation and never changes.
}
\examples{
m <- matrix(c(0,3,0, 1,0,2, 0,0,0), 3, 3, byrow = TRUE,
            dimnames = list(letters[1:3], letters[1:3]))
as.matrix(xsymmetrize(m))                  # max
as.matrix(xsymmetrize(m, method = "min"))
as.matrix(xsymmetrize(m, method = "difference"))
attr(xsymmetrize(campnet), "history")
}
\seealso{
\code{\link[=xtranspose]{xtranspose()}}, \code{\link[=xdichotomize]{xdichotomize()}}.
}
