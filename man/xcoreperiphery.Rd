% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xcoreperiphery.R
\name{xcoreperiphery}
\alias{xcoreperiphery}
\title{Core/periphery structure}
\usage{
xcoreperiphery(
  net,
  type = c("categorical", "continuous"),
  relation = NULL,
  diagonal = FALSE,
  starts = NULL,
  maxit = 500,
  c2p = NULL,
  p2c = NULL,
  seed = NULL,
  positive = TRUE
)
}
\arguments{
\item{net}{A network (any accepted form). 1-mode.}

\item{type}{Which model to print: \code{"categorical"} (the default) or
\code{"continuous"}.}

\item{relation}{Which relation of a multi-relation dataset, by name or
position. Defaults to the first.}

\item{diagonal}{Count the diagonal (self-ties)? \code{FALSE} by default, as in
both dialogs.}

\item{starts}{Number of starts for the categorical search, the first from
the degree-based partition. \code{NULL} follows UCINET: 20 below 50 nodes, 10
below 150, else 3.}

\item{maxit}{Maximum hill-climbing steps per start. UCINET's default is 500.}

\item{c2p, p2c}{Ideal density of the core-to-periphery and
periphery-to-core blocks, or \code{NULL} (the default) to ignore them.}

\item{seed}{Seed for the random starts. \code{NULL} draws one; it is reported.}

\item{positive}{Continuous model: report absolute coreness, UCINET's
\emph{Prevent negative coreness} (on by default).}
}
\value{
An object of class \code{c("xcoreperiphery", "xucinet_output")}.
\verb{$nodes}: \code{Coreness}, \code{InCore} and \code{Class}. \verb{$summary}: the categorical
and continuous fits, Gini coefficient, heterogeneity, recommended core
size and its concentration. \verb{$matrices}: the categorical block densities,
the concentration table and the continuous model's expected values. \verb{$fit}
is the fit of the printed model.
}
\description{
UCINET: Network | Core/Periphery | Categorical and Continuous. Finds the
core of a network, a set of nodes densely tied to each other, and the
periphery, loosely tied to each other.
}
\details{
Both models are always fitted; \code{type} chooses which is printed.

\strong{Categorical} (Borgatti and Everett 1999): a partition into core and
periphery that maximizes the correlation between the data and the ideal
pattern, a complete core block and an empty periphery block. The
core-to-periphery and periphery-to-core blocks are ignored unless \code{c2p} and
\code{p2c} give them a density. The search is UCINET's: hill climbing from a
degree-based start and then from \code{starts - 1} random partitions. \code{Class} in
\verb{$nodes} is 1 for the core and 2 for the periphery.

\strong{Continuous}: a coreness score for every node, such that the product of
two nodes' scores approximates the tie between them, estimated by MINRES
(UCINET's algorithm). \code{Coreness} holds the scores, normalized to unit
length; \code{InCore} marks the core UCINET recommends, the top nodes where the
correlation between the scores and a 0/1 core vector is highest (the
\emph{concentration}).
}
\section{UCINET equivalent}{

Network | Core/Periphery | Categorical | Borgatti & Everett: \emph{Exclude ties
to self} is \code{diagonal = FALSE}, \emph{Number of random starts} is \code{starts},
\emph{Max number of iterations} is \code{maxit}, \emph{Desired density for core to
periphery ties} is \code{c2p}, and \emph{periphery to core} is \code{p2c}.
Network | Core/Periphery | Continuous: \emph{Diagonal values valid} is
\code{diagonal}, \emph{Prevent negative coreness} is \code{positive}.
}

\examples{
xcoreperiphery(campnet, seed = 1)
xcoreperiphery(campnet, type = "continuous")
}
\seealso{
\code{\link[=xblockmodel]{xblockmodel()}}, \code{\link[=xeigenvector]{xeigenvector()}}.
}
