% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xplot.R
\name{xlayout}
\alias{xlayout}
\title{Node coordinates for a network drawing}
\usage{
xlayout(
  net,
  layout = "spring",
  attribute = NULL,
  data = NULL,
  relation = NULL,
  seed = 1
)
}
\arguments{
\item{net}{A network (any accepted form). 2-mode data are laid out as the
bipartite graph, row nodes first.}

\item{layout}{One of the methods above.}

\item{attribute}{For \code{"groups"}: the grouping, a vector or the name of an
attribute (looked up in \code{net}, then \code{data}).}

\item{data}{A data frame to look \code{attribute} up in.}

\item{relation}{Which relations the layout is computed on; all of them by
default.}

\item{seed}{Random seed for \code{"spring"}, \code{"kk"} and \code{"random"}. The default
is fixed, so the same call always gives the same picture; \code{NULL} draws a
new one each time.}
}
\value{
A two-column matrix of coordinates with the node labels as row
names, and the method in attribute \code{"xlayout"}.
}
\description{
Computes where \code{\link[=xplot]{xplot()}} puts the nodes, so that a layout can be kept and
reused: for the same network drawn at several cut-offs (book, 7.4.1), for a
subset of its nodes (7.5), or for the network at a later time (7.6),
\code{xplot(net2, layout = xlayout(net1))}.
}
\details{
The layout is computed on the underlying graph: every tie in any of the
chosen relations, symmetrized, values ignored.
\itemize{
\item \code{"spring"}: Fruchterman and Reingold's force-directed layout (book,
7.2.3), from igraph.
\item \code{"kk"}: Kamada and Kawai's layout, whose springs have the lengths of the
geodesic distances (7.2.3).
\item \code{"mds"}, \code{"nmds"}: classical or non-metric multidimensional scaling of the
geodesic distances, unreachable pairs set to one more than the largest
distance (7.2.2).
\item \code{"circle"}: the nodes on a circle, in their order (7.2).
\item \code{"random"}: uniformly at random (7.2, Figure 7.1).
\item \code{"groups"}: the nodes of each category of \code{attribute} together, the
categories around a circle (7.2.1, Figure 7.6).
\item \code{"bipartite"}: for 2-mode data, the row nodes in one line and the column
nodes in another.
}

A layout by attributes (7.2.1) needs no function: pass the attribute columns
themselves to \code{xplot(layout =)}.
}
\examples{
xy <- xlayout(campnet)
head(xy)
xplot(campnet, layout = xy)
}
\seealso{
\code{\link[=xplot]{xplot()}}.
}
