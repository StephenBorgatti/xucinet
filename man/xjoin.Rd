% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relations.R
\name{xjoin}
\alias{xjoin}
\title{Join datasets into one}
\usage{
xjoin(..., mode = c("col", "row", "mat"), names = NULL)
}
\arguments{
\item{...}{The datasets, or a single list of them. Names given here name
the relations.}

\item{mode}{\code{"col"} (the default) appends columns, so a 50 x 3 and a
50 x 4 make a 50 x 7; \code{"row"} appends rows; \code{"mat"} stacks the datasets
as the relations of one dataset, which is UCINET's Join Matrices and
needs them all the same shape.}

\item{names}{Relation names for \code{mode = "mat"}, if the argument names and
the titles are not what is wanted.}
}
\value{
An \code{xucinet} object. For \code{"mat"}, a multi-relation dataset; for the
other two, a single matrix.
}
\description{
UCINET: Data | Join. Puts several datasets together, either side by side,
one above the other, or as the relations of one multi-relation dataset.
}
\examples{
a <- matrix(1, 3, 2, dimnames = list(c("x","y","z"), c("p","q")))
b <- matrix(2, 3, 1, dimnames = list(c("x","y","z"), "r"))
as.matrix(xjoin(a, b))

# as relations instead
xrelations(xjoin(advice = campnet, friends = campnet, mode = "mat"))
}
\seealso{
\code{\link[=xunpack]{xunpack()}} to take one apart, \code{\link[=xcombine]{xcombine()}} to collapse the
relations into one matrix, and \code{\link[=xmatch]{xmatch()}} to line up datasets whose node
sets differ before joining them - which together are what UCINET's
Transform | Time Stack does.
}
