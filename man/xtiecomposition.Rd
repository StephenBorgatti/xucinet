% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xtiecomposition.R
\name{xtiecomposition}
\alias{xtiecomposition}
\title{Egonet tie composition}
\usage{
xtiecomposition(
  net,
  relations = NULL,
  ties = c("any", "both", "out", "in", "reciprocated", "equal"),
  op = c("!=", ">", ">=", "==", "<=", "<"),
  cutoff = 0,
  diagonal = FALSE
)
}
\arguments{
\item{net}{A network (any accepted form), usually with several relations.}

\item{relations}{Which relations to use, by name or position. \code{NULL}, the
default, uses them all, as UCINET does.}

\item{ties}{Which of ego's ties count: \code{"any"} (the default, a
tie in either direction counted once), \code{"both"} (in and out counted
separately, so a reciprocated tie counts twice), \code{"out"}, \code{"in"},
\code{"reciprocated"}, or \code{"equal"} (reciprocated with the same value both ways).}

\item{op, cutoff}{What counts as a tie: the value compared with \code{cutoff} by
\code{op}, one of \code{"!="} (the default), \code{">"}, \code{">="}, \code{"=="}, \code{"<="}, \code{"<"}.}

\item{diagonal}{Count ties to self? \code{FALSE} by default.}
}
\value{
An object of class \code{c("xtiecomposition", "xucinet_output")} with the
measures in \verb{$nodes}, in original node order.
}
\description{
UCINET: Network | Ego Networks | Egonet Tie Composition. For a network with
several relations - Sampson's liking, esteem and their negatives, say - how
each ego's ties are spread across them: the count on each relation, the
proportions, and how heterogeneous the mix is.
}
\details{
For each ego the columns are \code{Ties} (the total over every relation), then a
count \verb{f<relation>} and a proportion \verb{p<relation>} for each relation, then
\code{Blau}, one minus the sum of squared proportions, and \code{IQV}, Blau divided
by its maximum \code{1 - 1/R} for \code{R} relations. An ego with no ties has missing
proportions and heterogeneity.

On a single relation the count is just degree - out-degree, in-degree or
their union according to \code{ties} - and the proportions are trivially 1,
which the report notes.

A tie is any cell passing the test \code{op}/\code{cutoff} (by default, any value other
than 0). A zero is never a tie, whatever the test, because UCINET recodes
zeros to missing before testing.
}
\section{UCINET equivalent}{

Network | Ego Networks | Egonet Tie Composition. \emph{Which ties matter?} is
\code{ties}; the valid-ties operator and value are \code{op} and \code{cutoff};
\emph{Include ties to self} is \code{diagonal}.
}

\examples{
xtiecomposition(sampson)
}
\seealso{
\code{\link[=xvaluedtiecomposition]{xvaluedtiecomposition()}} for the values of the ties rather than
their count.
}
