% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xbipartite.R
\name{xbipartite}
\alias{xbipartite}
\title{Bipartite representation of a 2-mode network}
\usage{
xbipartite(
  net,
  relation = NULL,
  fill = 0,
  symmetric = TRUE,
  rowprefix = "",
  colprefix = ""
)
}
\arguments{
\item{net}{A 2-mode network (any accepted form).}

\item{relation}{Which relation, by name or position. Defaults to the first,
the only one UCINET converts.}

\item{fill}{The value of the row-by-row and column-by-column blocks. 0 by
default; \code{NA} for missing.}

\item{symmetric}{Put the transpose in the bottom-left block, making the
result undirected? \code{TRUE} by default.}

\item{rowprefix, colprefix}{Text put in front of the row and column labels.}
}
\value{
An \code{xucinet} object: 1-mode, \code{nr + nc} nodes, undirected when
\code{symmetric}, with \verb{$attributes$mode}.
}
\description{
UCINET: Transform | Graph Theoretic | Bipartite. Turns a 2-mode network of
\code{nr} rows by \code{nc} columns into a 1-mode network of \code{nr + nc} nodes, the
rows followed by the columns, in which a row node is tied to the column
nodes it had a tie with. Rows are never tied to rows, nor columns to
columns.
}
\details{
\verb{$attributes} has a \code{mode} column, \code{"row"} or \code{"col"}, so a plot or a
later routine can tell the two kinds of node apart.
}
\section{UCINET equivalent}{

Transform | Graph Theoretic | Bipartite. \emph{Value to fill blocks} is \code{fill},
\emph{Symmetric} is \code{symmetric}, the label prefixes are \code{rowprefix} and
\code{colprefix}.
}

\examples{
b <- xbipartite(davis)
xattributes(b)
}
\seealso{
\code{\link[=xaffiliations]{xaffiliations()}}, \code{\link[=xbicliques]{xbicliques()}}.
}
