% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xfastgreedy.R
\name{xfastgreedy}
\alias{xfastgreedy}
\title{Fast greedy community detection}
\usage{
xfastgreedy(net, relation = NULL)
}
\arguments{
\item{net}{A network (any accepted form). 1-mode. Directed data are
symmetrized by maximum; tie values are used as weights.}

\item{relation}{Which relation of a multi-relation dataset, by name or
position. Defaults to the first.}
}
\value{
An object of class \code{c("xfastgreedy", "xucinet_output")} with
\code{Cluster} in \verb{$nodes} and the number of clusters and modularity in
\verb{$summary}.
}
\description{
Clauset, Newman and Moore's agglomerative method: every node starts in its
own group, and the pair of groups whose merger raises modularity most is
merged, until no merger raises it. The partition with the highest
modularity is returned.
}
\details{
Computed by \code{igraph::cluster_fast_greedy()}. UCINET's own FastGreedy
(Network | Subgroups | FastGreedy) starts by default from a partition built
from cliques rather than from single nodes, so its answer can differ; see
the differences ledger.
}
\section{UCINET equivalent}{

Network | Subgroups | FastGreedy, with \emph{Initial Partition} set to Identity.
}

\examples{
xfastgreedy(campnet)
}
\seealso{
\code{\link[=xcommunities]{xcommunities()}}, \code{\link[=xlouvain]{xlouvain()}}.
}
