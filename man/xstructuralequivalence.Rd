% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xstructuralequivalence.R
\name{xstructuralequivalence}
\alias{xstructuralequivalence}
\title{Structural equivalence (profile similarity)}
\usage{
xstructuralequivalence(
  net,
  method = c("euclidean", "correlation", "matches", "positivematches", "overlaps",
    "crossproducts", "coverage"),
  relations = NULL,
  diagonal = c("reciprocal", "reciprocal2", "ignore", "retain", "retain2"),
  transpose = TRUE,
  geodesics = FALSE,
  k = NULL
)
}
\arguments{
\item{net}{A network (any accepted form). 1-mode.}

\item{method}{\code{"euclidean"} (UCINET's default), \code{"correlation"},
\code{"matches"}, \code{"positivematches"}, \code{"overlaps"}, \code{"crossproducts"} or
\code{"coverage"}.}

\item{relations}{Which relations make up the profile, by name or position.
\code{NULL}, the default, uses all of them, as UCINET does.}

\item{diagonal}{How the cells for the pair itself are treated; see Details.}

\item{transpose}{Add each node's column to its row? \code{TRUE} by default.}

\item{geodesics}{Compare rows of the geodesic distance matrix instead of the
adjacency matrix? Unreachable pairs are set to \code{n}, as UCINET does.}

\item{k}{Optional number of clusters: fills the \code{Cluster} column.}
}
\value{
An object of class \code{c("xstructuralequivalence", "xucinet_output")}.
\verb{$matrices} holds the structural equivalence matrix; \verb{$nodes} holds the
partition at each level of the clustering and \code{Cluster}; \verb{$hclust} the
tree.
}
\description{
UCINET: Network | Roles & Positions | Structural Equivalence | Profile.
Compares every pair of nodes on their profiles, the ties they send and
receive, and clusters the result. Two nodes are structurally equivalent
when their profiles are the same.
}
\details{
A node's profile is its row of the adjacency matrix, followed by its column
when \code{transpose = TRUE} (UCINET's default), for every relation of the
dataset in turn. How the cells that refer to the pair itself are handled is
\code{diagonal}:

\describe{
\item{\code{"reciprocal"}}{(the default) \emph{reciprocal swapping}: i's tie to j is
compared with j's tie to i, and i's self-loop with j's (UCINET's
Reciprocal1, single count).}
\item{\code{"reciprocal2"}}{the same, with the swapped cells counted again in
the column half of the profile (Reciprocal2, double count).}
\item{\code{"ignore"}}{the cells for i and j are left out.}
\item{\code{"retain"}, \code{"retain2"}}{the cells are compared as they stand,
once or twice (Retain1, Retain2).}
}

Without the transpose the single and double counts are the same, as in
UCINET.

The similarity matrix is clustered by Johnson's method with weighted-average
linkage, as UCINET does: its "weighted average" weights by cluster size,
which is \code{\link[=xhclust]{xhclust()}}'s \code{"average"}. For other linkages pass
\verb{$matrices[[1]]} to \code{\link[=xhclust]{xhclust()}} yourself. Coverage is asymmetric and is not
clustered.
}
\section{UCINET equivalent}{

Network | Roles & Positions | Structural Equivalence | Profile. \emph{Measure} is
\code{method}, \emph{Include transpose} is \code{transpose}, \emph{Diagonal} is \code{diagonal},
\emph{Use geodesics} is \code{geodesics}.
}

\examples{
xstructuralequivalence(sampson, relations = c("Esteem", "Disesteem"))
}
\seealso{
\code{\link[=xhclust]{xhclust()}}, \code{\link[=xsimilarities]{xsimilarities()}}, \code{\link[=xblockmodel]{xblockmodel()}}.
}
