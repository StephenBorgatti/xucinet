% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform.R
\name{xtranspose}
\alias{xtranspose}
\title{Transpose a network}
\usage{
xtranspose(net)
}
\arguments{
\item{net}{A network (any accepted form).}
}
\value{
An \code{xucinet} object, titled as UCINET names it (\code{campnet-Transp}),
with a \code{history} attribute. Multi-relation input is transposed relation by
relation and stays a stack.
}
\description{
UCINET: Transform | Transpose. Rows become columns and columns become rows.
For a directed 1-mode network this reverses every tie, turning "i chooses j"
into "i is chosen by j"; for a 2-mode network it swaps the two node sets.
}
\examples{
m <- matrix(c(0,1,1, 0,0,1, 0,0,0), 3, 3, byrow = TRUE,
            dimnames = list(letters[1:3], letters[1:3]))
xtranspose(m)

# 2-mode: the modes swap over
dim(davis)
dim(xtranspose(davis))
}
\seealso{
\code{\link[=xsymmetrize]{xsymmetrize()}} to fold the two directions together instead.
}
