% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-xucinet.R
\name{xrelations}
\alias{xrelations}
\alias{xnrelations}
\title{Relations in a dataset}
\usage{
xrelations(net)

xnrelations(net)
}
\arguments{
\item{net}{A network (any accepted form).}
}
\value{
\code{xrelations()} a character vector of relation names; \code{xnrelations()}
an integer.
}
\description{
UCINET datasets may hold several relations in one stack (advice, friendship,
reports). \code{xrelations()} gives their names and \code{xnrelations()} how many there
are; \code{length(xrelations(net))} is always \code{xnrelations(net)}. A dataset with a
single unnamed relation reports its own title as the relation name.
}
\examples{
m <- matrix(c(0,1,1, 1,0,0, 1,0,0), 3, 3)
xrelations(as_xucinet(list(liking = m, advice = t(m))))
xnrelations(m)
}
