% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xsimilarities.R
\name{xsimilarities}
\alias{xsimilarities}
\title{Similarities and distances between rows, columns or relations}
\usage{
xsimilarities(
  net,
  method = "correlation",
  mode = c("cols", "rows", "relations"),
  diagonal = FALSE
)
}
\arguments{
\item{net}{A network (any accepted form).}

\item{method}{One of \code{"correlation"} (the dialog's default), \code{"covariance"},
\code{"crossproducts"}, \code{"avgcrossproducts"}, \code{"matches"}, \code{"jaccard"},
\code{"valuedjaccard"}, \code{"identity"}, \code{"cosine"}, \code{"kappa"}, \code{"yulesq"},
\code{"euclidean"}, \code{"manhattan"}, \code{"avgabsdiff"}, \code{"nssd"}, \code{"nonmatches"},
\code{"jaccarddistance"}, \code{"hamming"} or \code{"ssd"}.}

\item{mode}{\code{"cols"} (the dialog's default), \code{"rows"}, or \code{"relations"} to
compare the relations of a multi-relation dataset with each other.}

\item{diagonal}{Are the diagonal values valid? \code{FALSE} by default, as the
dialog has it: the diagonal is set missing before the profiles are
compared, so cells \verb{(i,i)} and \verb{(j,j)} drop out of every pair. Square
1-mode data only; forced \code{TRUE} otherwise.}
}
\value{
An \code{xucinet} object: a square, symmetric, undirected dataset
labelled by whatever was compared, titled the way UCINET names it
(\code{campnet-Pea-C}). The \code{history} attribute says which measure was used
and whether it is a similarity or a distance, which is what
\code{\link[=xmds]{xmds()}} and \code{\link[=xhclust]{xhclust()}} will want to know.
}
\description{
UCINET: Tools | Similarities & Distances. Compares every pair of rows,
every pair of columns, or every pair of relations, and returns the square
matrix of the chosen measure.
}
\details{
Nineteen measures, the eleven similarities and eight distances the dialog
offers, ported cell for cell from \code{utsimilarity.pas} so the numbers match.
All of them delete pairwise: a cell counts only where both profiles have a
value.
}
\examples{
s <- xsimilarities(campnet)
round(as.matrix(s)[1:4, 1:4], 3)
attr(s, "history")

# Jaccard on the columns of a 2-mode dataset
round(as.matrix(xsimilarities(davis, method = "jaccard"))[1:4, 1:4], 3)
}
\seealso{
\code{\link[=xmds]{xmds()}} and \code{\link[=xhclust]{xhclust()}}, which take a proximity matrix;
\code{\link[=xgeodesic]{xgeodesic()}} for distances through the network rather than between
profiles.
}
