% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xgeodesic.R
\name{xgeodesic}
\alias{xgeodesic}
\title{Geodesic distances}
\usage{
xgeodesic(
  net,
  directed = NULL,
  reciprocal = FALSE,
  unreachable = NULL,
  diagonal = NULL
)
}
\arguments{
\item{net}{A network (any accepted form). 1-mode only.}

\item{directed}{\code{NULL} (detect), \code{TRUE} or \code{FALSE}. An undirected network is
symmetrized with the maximum first, so a tie either way is a step.}

\item{reciprocal}{Return \code{1 / distance} instead of the distance, UCINET's
"Reciprocal distances" transformation. Nearness rather than farness, so
unreachable pairs sit at the bottom of the scale rather than the top.}

\item{unreachable}{What to put where there is no path. \code{"missing"}, \code{"n"}
(the number of nodes) or \code{"max+1"} (one more than the longest distance
found); with \code{reciprocal = TRUE} those become \code{NA}, \code{1/n} and
\code{1/(max+1)}, and \code{"zero"} is offered as well. \code{NULL}, the default, takes
the dialog's: \code{"missing"} without the transformation and \code{"zero"} with
it.}

\item{diagonal}{What to put on the diagonal: \code{0}, \code{1} or \code{NA}. \code{NULL}, the
default, takes the dialog's: \code{0} without the transformation and \code{NA} with
it.}
}
\value{
An \code{xucinet} object titled \verb{<name>-geo}, with a \code{history} attribute
carrying the average and standard deviation of the off-diagonal
distances and the frequency table of distance values, which is what
UCINET's log prints beneath the matrix.
}
\description{
UCINET: Network | Cohesion | Geodesic Distances. The number of steps in the
shortest path between every pair of nodes.
}
\details{
The dialog takes a non-valued adjacency matrix, so valued data are
dichotomized at \verb{> 0} first and the history says so. Unreachable pairs and
the diagonal are filled in afterwards, exactly as \code{cleanup0}, \code{cleanup1}
and \code{setdiagonalvalues} do, and the defaults follow the transformation the
way the dialog's own defaults do.
}
\examples{
g <- xgeodesic(campnet)
as.matrix(g)[1:4, 1:4]
attr(g, "history")

# nearness instead of farness
as.matrix(xgeodesic(campnet, reciprocal = TRUE))[1:4, 1:4]
}
\seealso{
\code{\link[=xbetweenness]{xbetweenness()}}, which counts geodesics rather than measuring
them, and \code{\link[=xmds]{xmds()}}, which will scale the result.
}
