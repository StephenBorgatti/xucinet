% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform.R
\name{xrecode}
\alias{xrecode}
\title{Recode the values in a network}
\usage{
xrecode(
  net,
  from,
  to,
  diagonal = FALSE,
  rows = NULL,
  cols = NULL,
  relations = NULL
)
}
\arguments{
\item{net}{A network (any accepted form).}

\item{from}{Values to replace: a numeric vector of single values, a
two-column matrix of \verb{low, high} ranges, or a list mixing single values
and \code{c(low, high)} pairs.}

\item{to}{The replacement for each rule; either one value for all of them
or one per rule.}

\item{diagonal}{Recode the diagonal too? \code{FALSE} by default for square
data, as in the dialog, and forced on for a non-square matrix.}

\item{rows, cols}{Restrict the recoding to these rows or columns, by label
or index. \code{NULL} (the default) means all of them, UCINET's ALL.}

\item{relations}{Restrict it to these relations of a stack, by name or
position. \code{NULL} means all.}
}
\value{
An \code{xucinet} object titled \verb{<name>-Rec}, with a \code{history} attribute.
}
\description{
UCINET: Transform | Recode. Replaces cell values according to a schedule of
inclusive ranges.
}
\details{
Every rule is tested against the cell's \emph{original} value and the last
matching rule wins, which is how \code{recodedsl} resolves an overlapping
schedule. A cell matched by no rule keeps the value it had, and missing
cells are never recoded.
}
\examples{
m <- matrix(c(0,3,1, 2,0,0, 5,1,0), 3, 3, byrow = TRUE,
            dimnames = list(letters[1:3], letters[1:3]))
as.matrix(xrecode(m, from = c(3, 5), to = 9))
# a range: anything from 1 to 3 becomes 1
as.matrix(xrecode(m, from = list(c(1, 3)), to = 1))
}
\seealso{
\code{\link[=xdichotomize]{xdichotomize()}} for the binary case.
}
