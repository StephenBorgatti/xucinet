% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xfastgreedy.R
\name{xlabelpropagation}
\alias{xlabelpropagation}
\title{Label propagation}
\usage{
xlabelpropagation(net, seed = NULL, relation = NULL)
}
\arguments{
\item{net}{A network (any accepted form). 1-mode. Directed data are
symmetrized by maximum; tie values are used as weights.}

\item{seed}{A random seed, for a repeatable result. \code{NULL} uses R's current
random state. R's global random state is left as it was either way.}

\item{relation}{Which relation of a multi-relation dataset, by name or
position. Defaults to the first.}
}
\value{
An object of class \code{c("xlabelpropagation", "xucinet_output")} with
\code{Cluster} in \verb{$nodes} and the number of clusters and modularity in
\verb{$summary}.
}
\description{
Every node starts with its own label and repeatedly adopts the label most
common among its neighbours, until each node holds a label at least as
common around it as any other. Groups are the nodes sharing a label.
}
\details{
Computed by \code{igraph::cluster_label_prop()}. The method is random: it visits
the nodes in a random order and breaks ties at random, so different seeds can
give different partitions. UCINET's Label Propagation is random too, with its
own generator, so the two agree only by chance on networks without clear
groups.
}
\section{UCINET equivalent}{

Network | Subgroups | Label Propagation.
}

\examples{
xlabelpropagation(campnet, seed = 1)
}
\seealso{
\code{\link[=xcommunities]{xcommunities()}}.
}
