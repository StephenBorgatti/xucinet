% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xaltercomposition.R
\name{xaltercomposition}
\alias{xaltercomposition}
\title{Egonet alter composition}
\usage{
xaltercomposition(
  net,
  attribute,
  type = NULL,
  relation = NULL,
  ties = c("any", "out", "in", "reciprocated"),
  ignoreown = FALSE,
  weighting = c("analytic", "none", "multiply"),
  data = NULL
)
}
\arguments{
\item{net}{A network (any accepted form). 1-mode.}

\item{attribute}{The attribute: a vector, or the name of a column looked up
first in \code{net}'s attribute table and then in \code{data}.}

\item{type}{\code{"categorical"}, \code{"continuous"}, or \code{NULL} (the default) to
decide from the attribute.}

\item{relation}{Which relation of a multi-relation dataset, by name or
position. Defaults to the first.}

\item{ties}{Who counts as an alter: \code{"any"} (the default, a tie
in either direction), \code{"out"}, \code{"in"} or \code{"reciprocated"}.}

\item{ignoreown}{Categorical only: leave out alters in ego's own category?
\code{FALSE} by default.}

\item{weighting}{Continuous only: \code{"analytic"} (the default), \code{"none"} or
\code{"multiply"}.}

\item{data}{A data frame to look \code{attribute} up in.}
}
\value{
An object of class \code{c("xaltercomposition", "xucinet_output")} with
the measures in \verb{$nodes}, in original node order, and, for a categorical
attribute, the attribute's frequencies in \verb{$matrices$Frequencies}.
}
\description{
UCINET: Network | Ego Networks | Egonet Alter Composition, both the
Categorical and the Continuous item, as one function. Describes the alters
of each ego on an attribute: for a categorical attribute, how many fall in
each category and how mixed they are; for a continuous one, their average,
spread and range.
}
\details{
Whether the attribute is categorical is decided by \code{type}, or when that is
\code{NULL} by the attribute itself: character, factor and logical values are
categorical, numbers are continuous. A numeric attribute with five or fewer
whole-number values is probably a set of category codes (camp92's \code{Gender}
is 1 and 2), and the report says so, but it is still treated as continuous
unless \code{type = "categorical"} is given.

\strong{Categorical.} The first column is ego's own value. Then, for each
category \code{c} (sorted), \code{fc} is how many alters are in it and \code{pc} what
proportion of ego's alters that is, followed by \code{Heterogeneity} (Blau's
index, one minus the sum of squared proportions) and \code{IQV} (Blau divided by
its maximum, \code{1 - 1/K} for the \code{K} categories of the whole attribute). Tie
values weight the counts, as UCINET does: on valued data \code{fc} is the total
strength of ego's ties into category \code{c}. A table of the attribute's own
frequencies is printed first, as UCINET prints it.

\strong{Continuous.} \code{Avg}, \code{Sum}, \code{Min}, \code{Max}, \code{StdDev} (dividing by the
number of alters), \code{EstSD} (dividing by one less), \code{CV} (StdDev over Avg),
\code{Num} (the number of alters) and \code{WtdNum} (the sum of their tie weights).
With the default \code{weighting = "analytic"} the tie values weight the mean and
the standard deviations; \code{"multiply"} multiplies each alter's value by the
tie before anything else; \code{"none"} ignores tie values.
}
\section{UCINET equivalent}{

Network | Ego Networks | Egonet Alter Composition | Categorical, and ... |
Continuous. \emph{Definition of Ego Network} is \code{ties}, \emph{Ignore ego's own
category} is \code{ignoreown}, and the continuous dialog's \emph{Valued tie data} is
\code{weighting}.
}

\examples{
xaltercomposition(campnet, camp92_attr$Gender, type = "categorical")
xaltercomposition(hightech, "Age", data = hightech_attr)
}
\seealso{
\code{\link[=xegoaltersimilarity]{xegoaltersimilarity()}} for whether alters resemble ego.
}
