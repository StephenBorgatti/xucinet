% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xhomophily.R
\name{xhomophily}
\alias{xhomophily}
\title{Whole-network homophily}
\usage{
xhomophily(
  net,
  attribute,
  directed = NULL,
  weighted = TRUE,
  diagonal = FALSE,
  data = NULL
)
}
\arguments{
\item{net}{A network (any accepted form). 1-mode only.}

\item{attribute}{The grouping: a vector, or the name of a column looked up
first in \code{net}'s attribute table and then in \code{data} (G3).}

\item{directed}{\code{NULL} (detect), \code{TRUE}, or \code{FALSE} to ignore the direction
of ties, which is the dialog's checkbox.}

\item{weighted}{Treat the data as valued? \code{TRUE}, as the dialog does
(\code{TypeOfData.ItemIndex = 1}). \code{FALSE} dichotomizes at > 0 before every
measure. UCINET's \code{calcwhomophily} applies the choice to the mixing
matrix only, leaving \code{H}, \code{h-star}, \code{Corr}, \verb{Yules Q} and \verb{E-I Index}
on the raw values either way; that is UCINET issue 21, fix pending, and
xucinet does what the dialog says.}

\item{diagonal}{Allow reflexive ties? \code{FALSE} by default.}

\item{data}{A data frame to look \code{attribute} up in.}
}
\value{
An object of class \code{c("xhomophily", "xucinet_output")}, with the
seven measures in \verb{$summary} under UCINET's headings and the
within/between mixing matrix in \verb{$matrices}.
}
\description{
UCINET: Network | Whole Networks | Homophily/Influence | Categorical. Seven
ways of asking whether ties fall within groups more than between them.
}
\details{
\itemize{
\item \code{H} is the share of tie strength that stays inside a group, and
\code{h-star} subtracts what the group sizes would give by chance.
\item \verb{E-I Index} runs the other way: external minus internal over the total,
so it is +1 when every tie crosses a boundary and -1 when none does.
\item \code{Corr} correlates the tie value with whether the pair share a group;
\verb{Yules Q} does the same on the two-by-two table of tie against sameness.
\item \verb{Modul Q} is Newman modularity and \code{Assort} his categorical
assortativity, both computed on the mixing matrix.
}
}
\examples{
gender <- camp92_attr$Gender
names(gender) <- rownames(camp92_attr)
xhomophily(campnet, gender)
}
\seealso{
\code{\link[=xdensitybygroups]{xdensitybygroups()}} for the densities the mixing matrix implies,
and \code{\link[=xattributetomatrix]{xattributetomatrix()}}.
}
