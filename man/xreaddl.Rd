% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-dl.R
\name{xreaddl}
\alias{xreaddl}
\title{Read a DL file}
\usage{
xreaddl(file, directed = NULL, mode = NULL, title = NULL, ...)
}
\arguments{
\item{file}{Path to the \code{.dl} file.}

\item{directed, mode, title}{Passed to \code{\link[=as_xucinet]{as_xucinet()}}.}

\item{...}{Reserved.}
}
\value{
An \code{xucinet} object.
}
\description{
Reads UCINET's DL text format. The formats understood are \code{fullmatrix},
\code{edgelist1}, \code{edgelist2}, \code{nodelist1}, \code{nodelist2}, \code{lowerhalf} and
\code{upperhalf}, with \code{n}/\code{nr}/\code{nc} for shape, \code{nm} (also \code{nl}, \code{nmat}, \code{nrel},
\code{nresp}) for a stack of matrices, \verb{diagonal absent}, and \code{labels},
\verb{row labels}, \verb{column labels} and \verb{matrix labels} blocks. Keywords are
matched on a prefix and are not case sensitive, as UCINET matches them.
}
\examples{
f <- file.path(tempdir(), "demo.dl")
xsavedl(matrix(c(0,1,1, 1,0,0, 1,0,0), 3, 3), f)
xreaddl(f)
}
\seealso{
\code{\link[=xsavedl]{xsavedl()}}
}
