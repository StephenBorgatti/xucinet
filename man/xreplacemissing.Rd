% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ximpute.R
\name{xreplacemissing}
\alias{xreplacemissing}
\title{Fill missing ties from another dataset}
\usage{
xreplacemissing(net, source = net, transpose = TRUE)
}
\arguments{
\item{net}{A network (any accepted form).}

\item{source}{Where the replacements come from. The network itself by
default.}

\item{transpose}{Transpose the source first? \code{TRUE}, as the dialog has it.}
}
\value{
An \code{xucinet} object titled \verb{<name>-rna}, with a \code{history} attribute
recording how many cells were filled.
}
\description{
UCINET: Transform | Replace Missing Values. Takes the values for the
missing cells from a second dataset of the same shape.
}
\details{
The dialog's "Matrix containing replacement values needs to be transposed"
box is checked by default, and the source may be the input itself - which
is reconstruction from the transpose, the same thing
\code{ximpute(method = "re")} does.
}
\examples{
m <- matrix(c(0,1,1, NA,0,1, 0,NA,0), 3, 3, byrow = TRUE,
            dimnames = list(letters[1:3], letters[1:3]))
as.matrix(xreplacemissing(m))
}
\seealso{
\code{\link[=ximpute]{ximpute()}}.
}
