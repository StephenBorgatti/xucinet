% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-ucinet.R
\name{xreaducinet}
\alias{xreaducinet}
\title{Read a UCINET dataset}
\usage{
xreaducinet(file, directed = NULL, mode = NULL, title = NULL, ...)
}
\arguments{
\item{file}{Path to the \code{.##h} file, the \code{.##d} file, or the name with no
extension.}

\item{directed, mode, title}{Passed to \code{\link[=as_xucinet]{as_xucinet()}}. \code{title} defaults to the
title stored in the file, or the file name when the file has none.}

\item{...}{Reserved.}
}
\value{
An \code{xucinet} object; multi-relation datasets get one named matrix per
relation, named from the file's matrix labels.
}
\description{
Reads UCINET's native \code{.##h} / \code{.##d} pair, in pure R, for every header
version UCINET has written: 4010, 4020, 5000, 6000, 6404 and 6405. Labels,
the dataset title, multi-relation stacks, 2-mode shapes and missing values
all come across.
}
\details{
UCINET stores a missing cell as 1e38 and treats anything at or above 1e37 as
missing; those become \code{NA}. Values are stored to the precision of the file's
own datatype, which for almost every UCINET dataset is a 4-byte single.
}
\examples{
f <- system.file("goldens", "ucinet", "campnet.##h", package = "xucinet")
xreaducinet(f)
}
\seealso{
\code{\link[=xsaveucinet]{xsaveucinet()}} to write one, and \code{\link[=xread]{xread()}} which dispatches here.
}
