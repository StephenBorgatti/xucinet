% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xcloseness.R
\name{xcloseness}
\alias{xcloseness}
\title{Closeness centrality}
\usage{
xcloseness(
  net,
  relation = NULL,
  directed = NULL,
  undefined = c("max1", "n", "zero", "avg")
)
}
\arguments{
\item{net}{A network (any accepted form).}

\item{relation}{Which relation of a multi-relation dataset, by name or
position. Defaults to the first.}

\item{directed}{\code{NULL} (detect from symmetry), \code{TRUE} or \code{FALSE}.}

\item{undefined}{What to do with unreachable pairs: \code{"max1"} (default),
\code{"n"}, \code{"zero"} or \code{"avg"}.}
}
\value{
An \code{xucinet_output} whose \verb{$nodes} holds the three measures, or six
for directed data.
}
\description{
UCINET: Network | Centrality | Closeness. How near a node is to all the
others.
}
\details{
UCINET's dialog has no method selector: it reports \strong{three} measures at once,
each with its own options, and prints which options it used. This function
does the same, and its defaults are UCINET's defaults, confirmed from the
golden run rather than chosen here:

\describe{
\item{\code{FreeClo}}{Freeman closeness. Unreachable pairs are set to the
maximum observed distance plus one, then the score is \code{(n - 1) / sum(d)}.}
\item{\code{ValClo}}{Valente-Forman reverse distance. \code{diameter + 1 - d}, zero
where unreachable, averaged over \code{n - 1}, then divided by the diameter.}
\item{\code{RecipClo}}{Reciprocal distance. \code{1/d}, zero where unreachable,
averaged over \code{n - 1}.}
}

Directed data gives six columns, an out and an in version of each, and
UCINET's naming is not quite parallel: the Freeman pair are \code{OutClose} and
\code{InClose}, not \code{OutFreeClo}.

\code{undefined} covers the case SPEC D7 flags as a danger zone. The four values
are UCINET's four, in its own words: \code{"max1"} is \emph{max observed distance plus
1}, \code{"n"} is \emph{N (number of nodes)}, \code{"zero"} is \emph{ignore unreachables} and
computes closeness within components, \code{"avg"} substitutes the mean observed
distance. They apply to the Freeman measure; the other two have their own
fixed conventions, as UCINET's dialog does.
}
\examples{
xcloseness(campnet)
}
