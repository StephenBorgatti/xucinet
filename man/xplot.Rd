% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xplot.R
\name{xplot}
\alias{xplot}
\title{Draw a network}
\usage{
xplot(
  net,
  layout = "spring",
  relation = NULL,
  nodecolor = NULL,
  nodesize = NULL,
  nodeshape = NULL,
  label = TRUE,
  labelsize = NULL,
  edgewidth = NULL,
  edgecolor = NULL,
  edgestyle = NULL,
  arrows = NULL,
  arrowsize = NULL,
  cutoff = NULL,
  keep = NULL,
  ego = NULL,
  isolates = TRUE,
  legend = TRUE,
  data = NULL,
  seed = 1,
  main = NULL,
  file = NULL,
  width = 7,
  height = 7,
  dpi = 300,
  ...
)
}
\arguments{
\item{net}{A network (any accepted form). 2-mode data are drawn as the
bipartite graph, row nodes as circles and column nodes as squares.}

\item{layout}{A method name for \code{\link[=xlayout]{xlayout()}}, or coordinates: the result of
\code{\link[=xlayout]{xlayout()}} or of an earlier \code{xplot()}, or any two-column matrix or data
frame (for instance two attribute columns, Figure 7.5) with the node
labels as row names or in node order. Coordinates are matched by label, so
a layout of a larger network serves a subset of its nodes.}

\item{relation}{Which relations to draw: the first by default, or several
by name or position.}

\item{nodecolor, nodesize, nodeshape, labelsize}{Node attributes to draw; see
"How attributes are drawn".}

\item{label}{\code{TRUE} (node labels), \code{FALSE}, or a character vector of labels.}

\item{edgewidth}{Line width: a number, or an n by n matrix. \code{NULL} follows
the tie values when they are not all equal.}

\item{edgecolor}{Line colour; one per relation when several are drawn,
plus one for ties in several relations.}

\item{edgestyle}{Line style (\code{"solid"}, \code{"dashed"}, \code{"dotted"},
\code{"dotdash"}, ...), one per relation when several are drawn.}

\item{arrows}{Draw arrowheads? \code{NULL} draws them when a drawn relation is
not symmetric.}

\item{arrowsize}{Size of the arrowheads: a number (1 is the default size),
an n by n matrix, or \code{TRUE} to make them follow the tie values
(Figure 7.19).}

\item{cutoff}{Draw only ties whose value is greater than this (7.4.1).}

\item{keep}{The nodes to draw: a logical vector, a vector of labels, or
\code{NULL} for all (7.5.2).}

\item{ego}{Draw only the ego network of this node: it, the nodes it is tied
to in either direction, and the ties among them (7.5.3).}

\item{isolates}{\code{FALSE} leaves out the nodes that have no drawn tie, after
the layout is made, so the others keep their places (7.5.1).}

\item{legend}{Draw legends for categorical colours and shapes and for
relations?}

\item{data}{A data frame in which attribute names are looked up.}

\item{seed}{Random seed for the layout; see \code{\link[=xlayout]{xlayout()}}.}

\item{main}{A title.}

\item{file}{Write the drawing to this file instead of the screen: \code{.png},
\code{.jpg}, \code{.tiff}, \code{.pdf} or \code{.svg}, by the extension (7.7).}

\item{width, height}{Size of the file in inches.}

\item{dpi}{Resolution of a bitmap file.}

\item{...}{Passed to \code{\link[=xlayout]{xlayout()}} when \code{layout} is a method name, such as
\code{attribute} for \code{layout = "groups"}.}
}
\value{
The coordinates of the drawn nodes, invisibly, in the form
\code{\link[=xlayout]{xlayout()}} returns, so that the next drawing can reuse them.
}
\description{
The network drawing of book chapter 7, in base graphics. Nodes are drawn as
symbols whose colour, size and shape can show attributes (7.3), ties as
lines whose width, darkness and style can show tie values and relations
(7.4), and any subset of nodes or ties can be left out while the rest keep
their places (7.5).
}
\section{How attributes are drawn}{

\code{nodecolor}, \code{nodesize}, \code{nodeshape} and \code{labelsize} each take the name of
an attribute (looked up in \code{net}, then in \code{data}), a vector with one value
per node (named by node label, or in node order), or a node-level result
such as \code{xdegree(net)}, whose first numeric column is used.
\itemize{
\item Colour: categories get distinct colours and a legend; a continuous
attribute gets shades of grey, darker for larger values (Figure 7.15). A
colour name, such as \code{"white"}, colours every node.
\item Size: the symbol's diameter runs linearly from 1 to 3 times the default
between the smallest and the largest value.
\item Shape: categories get circle, square, diamond, triangle, down-triangle,
in that order. A shape name gives every node that shape.
\item Label size: from 0.6 to 1.6 times the default, linearly.
}

Text, factors, logicals and numbers with at most six distinct whole values
are treated as categories; wrap a variable in \code{\link[=factor]{factor()}} to force it.

Tie values: when the drawn ties are valued and \code{edgewidth} is not given,
line width (1 to 4) and darkness follow the value (Figures 7.5, 7.7).

Several relations (\code{relation = c("Marriage", "Business")}) are drawn
together, one colour and line style per relation, and ties present in more
than one relation as thicker lines in the last colour (Figure 7.20).
}

\section{UCINET equivalent}{

NetDraw. The drawing is not NetDraw's: its layout is not reproduced.
}

\examples{
xplot(campnet)
xplot(hightech, relation = "Friendship", nodesize = "Tenure",
      nodecolor = "Level", nodeshape = "Level", data = hightech_attr)
xplot(padgett, relation = c("Marriage", "Business"),
      edgecolor = c("darkgreen", "blue", "black"))
xy <- xplot(zachary, relation = "Strength", nodecolor = "Club", data = zachary_attr)
xplot(zachary, relation = "Strength", layout = xy, cutoff = 3, isolates = FALSE)
}
\seealso{
\code{\link[=xlayout]{xlayout()}}.
}
