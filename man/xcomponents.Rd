% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xcomponents.R
\name{xcomponents}
\alias{xcomponents}
\title{Components}
\usage{
xcomponents(net, type = c("weak", "strong"), min = 1, sortby = c("size", "id"))
}
\arguments{
\item{net}{A network (any accepted form). For 2-mode data the bipartite
graph is used, so a component may contain nodes of both modes.}

\item{type}{\code{"weak"} (the dialog's default) or \code{"strong"}.}

\item{min}{Report only components of at least this size. Nodes in smaller
ones come back \code{NA}. UCINET's dialog has no such option; this is an
addition, and \code{min = 1} is the behaviour it has.}

\item{sortby}{Order the size table by \code{"size"} (the dialog's default) or by
\code{"id"}.}
}
\value{
An object of class \code{c("xcomponents", "xucinet_output")}. \verb{$nodes}
holds the component of each node in the original order; \verb{$summary} the
count and the five heterogeneity figures; \verb{$matrices$Sizes} the size
table.
}
\description{
UCINET: Network | Regions | Components. The pieces a network falls into,
with the membership per node and UCINET's heterogeneity figures.
}
\details{
A \strong{weak} component ignores the direction of ties; a \strong{strong} one
requires each node to reach every other and be reached back. Isolates are
components of size 1, as in UCINET.

\verb{Normalized heterogeneity} is the fragmentation of the network - the
proportion of ordered pairs that fall in different components - which is
the identity UCINET's own log points out.
}
\examples{
xcomponents(campnet)
xcomponents(campnet, type = "strong")
}
\seealso{
\code{\link[=xcohesion]{xcohesion()}}, whose \code{Components} is the strong count, and
\code{\link[=xgeodesic]{xgeodesic()}}.
}
