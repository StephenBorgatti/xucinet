% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-uci.R
\name{xsaveuci}
\alias{xsaveuci}
\title{Write a .uci dataset}
\usage{
xsaveuci(
  net,
  file,
  layout = NULL,
  attributes = NULL,
  title = NULL,
  datatype = "single",
  pretty = TRUE
)
}
\arguments{
\item{net}{A network (any accepted form).}

\item{file}{Output path. \code{.uci} is appended if it has no extension.}

\item{layout}{\code{"matrix"} for a dense payload, \code{"edgelist"} for a sparse one,
or \code{NULL} (default) to pick whichever is smaller. The words are the ones
\code{\link[=xread]{xread()}} already uses, so there is one vocabulary rather than two.}

\item{attributes}{Optional data frame of node attributes, keyed by node
label. Defaults to \code{net$attributes} when the network carries one.}

\item{title}{Dataset title stored in the file. Defaults to the network's.}

\item{datatype}{Advisory hint for a reader converting back to \verb{##h}/\verb{##d},
which has to choose a fixed cell width. Ignored when reading.}

\item{pretty}{Indent the JSON? Readable but larger.}
}
\value{
The path written, invisibly.
}
\description{
Writes the single-file JSON format that replaces the \verb{##h}/\verb{##d} pair. This
is what \code{\link[=xsave]{xsave()}} uses when no other type is asked for.
}
\examples{
f <- file.path(tempdir(), "demo.uci")
m <- matrix(c(0,1,1, 1,0,0, 1,0,0), 3, 3)
xsaveuci(m, f)
xreaduci(f)
}
\seealso{
\code{\link[=xreaduci]{xreaduci()}}
}
