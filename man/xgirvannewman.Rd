% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xgirvannewman.R
\name{xgirvannewman}
\alias{xgirvannewman}
\title{Girvan-Newman clustering}
\usage{
xgirvannewman(net, k = 10, relation = NULL)
}
\arguments{
\item{net}{A network (any accepted form). 1-mode. Directed data are
symmetrized by maximum; values are ignored.}

\item{k}{The most clusters to go to. UCINET's default is 10.}

\item{relation}{Which relation of a multi-relation dataset, by name or
position. Defaults to the first.}
}
\value{
An object of class \code{c("xgirvannewman", "xucinet_output")}.
}
\description{
UCINET: Network | Subgroups | Girvan-Newman. Divides the network by
repeatedly cutting the ties with the highest edge betweenness, the ones
most shortest paths run along, and records the partition into components
each time the number of components grows.
}
\details{
\verb{$matrices$Partitions} has one column per partition, headed as UCINET heads
them (\code{C2}, \code{C3}, ...), from fewest clusters to most, and
\verb{$matrices$Modularity} the modularity of each. \code{Cluster} in the node table is
the partition with the highest modularity, and \verb{$summary} reports that one.

Ties in edge betweenness are removed together, as in UCINET. igraph's
\code{cluster_edge_betweenness()} removes them one at a time, so on networks with
such ties the two can pass through different partitions.
}
\section{UCINET equivalent}{

Network | Subgroups | Girvan-Newman. \emph{Output partitions with no more than
... clusters} is \code{k}.
}

\examples{
xgirvannewman(campnet)
}
\seealso{
\code{\link[=xcommunities]{xcommunities()}}, \code{\link[=xlouvain]{xlouvain()}}.
}
