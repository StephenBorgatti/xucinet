% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xbetweenness.R
\name{xbetweenness}
\alias{xbetweenness}
\title{Freeman betweenness centrality}
\usage{
xbetweenness(net, relation = NULL, directed = NULL, normalize = FALSE)
}
\arguments{
\item{net}{A network (any accepted form).}

\item{relation}{Which relation of a multi-relation dataset, by name or
position. Defaults to the first.}

\item{directed}{\code{NULL} (detect from symmetry), \code{TRUE} or \code{FALSE}.}

\item{normalize}{Selects which column \code{\link[=summary]{summary()}} reports as the headline.
Both are always in \verb{$nodes}.}
}
\value{
An \code{xucinet_output}. \verb{$nodes} has \code{Betweenness} and \code{nBetweenness};
\verb{$summary} has the un-normalized centralization and the network
centralization index, as UCINET prints both.
}
\description{
UCINET: Network | Centrality | Freeman Betweenness | Node Betweenness. How
often a node lies on the shortest path between two others.
}
\details{
Ported from \code{XFreeBet.pas}. The data are dichotomized at \verb{> 0} and the
diagonal ignored before anything else happens, which is what UCINET's
\code{copyfromtmat} does, so a valued matrix is treated as the graph underneath it.

Two conventions here are worth stating because they are easy to get subtly
wrong. UCINET computes the directed count and halves it for symmetric data,
but normalizes the \strong{un-halved} count by \code{(n-1)(n-2)}, which comes to the
same thing as the usual undirected formula and differs from it by a factor of
two if you take the halved value by mistake. And the normalized column is a
percentage, not a proportion, unlike the centralization figure \code{xdegree()}
reports.
}
\examples{
xbetweenness(campnet)
}
