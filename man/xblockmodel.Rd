% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xblockmodel.R
\name{xblockmodel}
\alias{xblockmodel}
\title{Blockmodel}
\usage{
xblockmodel(
  net,
  partition,
  relations = NULL,
  method = c("average", "count", "maximum", "minimum", "sd", "sum"),
  diagonal = FALSE,
  cutoff = NULL,
  data = NULL
)
}
\arguments{
\item{net}{A network (any accepted form). 1-mode.}

\item{partition}{The groups: a vector with one value per node, the name of
an attribute (looked up in \code{net}'s attributes and then in \code{data}), or a
result object whose \verb{$nodes} has a \code{Cluster} column.}

\item{relations}{Which relations to block, by name or position. \code{NULL}, the
default, blocks every relation, as UCINET does.}

\item{method}{The block summary: \code{"average"} (UCINET's default, the density
for binary data), \code{"count"}, \code{"maximum"}, \code{"minimum"}, \code{"sd"} or \code{"sum"}.}

\item{diagonal}{Include the diagonal (self-ties)? \code{FALSE} by default.}

\item{cutoff}{Where the image matrix puts a 1: a block value at or above
it. \code{NULL}, the default, uses each relation's density.}

\item{data}{A data frame to look \code{partition} up in.}
}
\value{
An object of class \code{c("xblockmodel", "xucinet_output")}.
\verb{$nodes} holds \code{Block}, the group of each node; \verb{$matrices} a table of
block values and an image matrix per relation; \verb{$summary} the
autocorrelation and the cutoff per relation.
}
\description{
UCINET: Transform | Aggregate | Block - Aggregate by Partitions. Summarizes
a network by a partition of its nodes: the matrix rearranged so that each
group's rows and columns are together, the density (or other summary) of
every block, the image matrix, and how well the blocks fit the data.
}
\details{
The partition is given, not found: an attribute, a clustering, or any
routine's result with a \code{Cluster} column. To search for the best partition,
see \code{xblockoptimize()} (not yet written; issue #24).

For each relation the report shows the blocked matrix and the table of
block values, and \verb{$summary} holds the \emph{autocorrelation}, UCINET's fit: the
correlation, over all cells, between a cell's value and its block's value.
The image matrix sets a block to 1 when its value reaches \code{cutoff}; by
default the cutoff is the relation's density (the alpha criterion), so a
1-block is one denser than the network as a whole.
}
\section{UCINET equivalent}{

Transform | Aggregate | Block - Aggregate by Partitions. \emph{Method} is
\code{method}, \emph{Utilize diagonal} is \code{diagonal}. UCINET prints no image matrix;
it is added here (ledger entry 34).
}

\examples{
xblockmodel(campnet, camp92_attr$Gender)
}
\seealso{
\code{\link[=xcombinenodes]{xcombinenodes()}}, \code{\link[=xdensitybygroups]{xdensitybygroups()}}, \code{\link[=xstructuralequivalence]{xstructuralequivalence()}}.
}
