% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xmatch.R
\name{xmatch}
\alias{xmatch}
\title{Line several datasets up on the same nodes}
\usage{
xmatch(
  ...,
  nodes = c("first", "intersection", "union", "last"),
  by = "rownames",
  sort = c("first", "alphabetical", "numerical"),
  fill = 0,
  attach = FALSE
)
}
\arguments{
\item{...}{The datasets: networks, attribute data frames, or a mix. Names
given here name the elements of the result.}

\item{nodes}{Which node set to use: \code{"first"} (the default, UCINET's
"primary"), \code{"last"}, \code{"intersection"} (only nodes every dataset has) or
\code{"union"} (every node any of them has).}

\item{by}{For data frames, the column holding the labels, or \code{"rownames"}
(the default).}

\item{sort}{The order: \code{"first"} (the default, the order they appear in),
\code{"alphabetical"} or \code{"numerical"}.}

\item{fill}{What to put in a network cell for a node the network did not
have. \code{0}, as UCINET allocates; \code{NA} gives Time Stack's behaviour.}

\item{attach}{Return the first network with the attribute table attached as
its \verb{$attributes}, instead of the list. Only for a call of one network
and one data frame.}
}
\value{
A named list, in argument order, of the datasets on the common node
set. Networks keep their class, mode, directedness, relation stack and
title; data frames stay data frames.
}
\description{
UCINET: Data | Match Datasets. Puts any number of networks and attribute
tables on one node set, in one order, so that they can be used together.
}
\details{
Labels are matched exactly, including case, as UCINET's are.

A node that a network did not have arrives with \code{fill} in its row and
column - zero by default, which is what UCINET's matrices are allocated
with. A node that an attribute table did not have arrives as \code{NA}
throughout instead: an unobserved tie is reasonably a zero, but an
unobserved attribute is not.
}
\examples{
a <- matrix(1, 3, 3, dimnames = list(c("x","y","z"), c("x","y","z")))
b <- matrix(2, 3, 3, dimnames = list(c("y","z","w"), c("y","z","w")))
m <- xmatch(a, b, nodes = "union")
as.matrix(m[[1]])
}
\seealso{
\code{\link[=xjoin]{xjoin()}} to stack the result into one multi-relation dataset,
which is what UCINET's Time Stack does.
}
