% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xautoregression.R
\name{xautoregression}
\alias{xautoregression}
\title{Network autoregression}
\usage{
xautoregression(
  net,
  formula,
  data = NULL,
  model = c("lag", "error"),
  normalize = TRUE,
  relation = NULL
)
}
\arguments{
\item{net}{A network (any accepted form), 1-mode: the weight matrix \eqn{W}.
Row \eqn{i} says whose values influence node \eqn{i}.}

\item{formula}{A model formula, \code{y ~ x1 + x2}.}

\item{data}{Where the variables are: a data frame (rows named by node
label, or in node order), a node-level result, or a network with
attributes. \code{NULL} uses \code{net}'s own attribute table, then the place the
formula was written.}

\item{model}{\code{"lag"} (the default) or \code{"error"}.}

\item{normalize}{Row-normalize \eqn{W}? \code{TRUE} by default. Rows of isolates
stay zero.}

\item{relation}{Which relation of a multirelational network is \eqn{W}.}
}
\value{
An object of class \code{c("xautoregression", "xucinet_output")}.
\verb{$matrices} has the coefficient table (\code{Coef}, \code{SE}, \code{Z}, \code{Sig}),
the network parameter (\code{Rho} or \code{Lambda}) in its last row. \verb{$summary} has
the number of nodes, sigma, the log-likelihood of the model and of the
intercept-only null model, and the AIC and BIC. \verb{$nodes} has the fitted
values and residuals.
}
\description{
Regression of a node attribute on others when the nodes' values depend on
one another through the network (book, 14.4). The \strong{lag} model (network
effects) is
\deqn{y = \rho W y + X\beta + \epsilon,}
in which each node's value is pulled toward the values of the nodes it is
tied to; the \strong{error} model (network disturbances) is
\deqn{y = X\beta + u, \quad u = \lambda W u + \epsilon,}
in which the unexplained parts are correlated along the ties. \eqn{W} is the
network, row-normalized by default. The model is fitted by maximum
likelihood with \code{\link[sna:lnam]{sna::lnam()}}, which this function wraps; the \code{sna} and
\code{numDeriv} packages must be installed.
}
\section{UCINET equivalent}{

None. UCINET has no network autoregression routine (see the differences
ledger).
}

\examples{
\donttest{
if (requireNamespace("sna", quietly = TRUE) &&
    requireNamespace("numDeriv", quietly = TRUE)) {
  deg <- xdegree(campnet)$nodes
  d <- data.frame(indeg = deg[["Indeg"]], btw = xbetweenness(campnet)$nodes[[1]],
                  row.names = rownames(deg))
  xautoregression(campnet, indeg ~ btw, d)
}
}
}
\seealso{
\code{\link[=xregression]{xregression()}} for the same regression without the network term.
}
