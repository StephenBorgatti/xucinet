% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-xucinet.R
\name{xattributes}
\alias{xattributes}
\title{Node attributes carried by a dataset}
\usage{
xattributes(net)
}
\arguments{
\item{net}{A network (any accepted form).}
}
\value{
A data frame of attributes keyed by node label, or \code{NULL} when the
dataset carries none.
}
\description{
The \code{.uci} format is a single-file container, so it can hold node attributes
alongside the network and a reader has to put them somewhere. They live in
\code{net$attributes} as an ordinary data frame keyed by node label, and this is
the supported way to reach them.
}
\details{
SPEC D1 keeps attributes out of the network object, and that still holds for
analysis: no routine reads this, every function that wants covariates takes
them as its own argument, and the slot is \code{NULL} for a network that arrived
any other way. It exists so that a \code{.uci} file round trips whole.
}
\examples{
f <- system.file("schema", "campnet-example.uci", package = "xucinet")
xattributes(xreaduci(f))
xattributes(campnet)   # NULL: campnet came from a ##h pair
}
\seealso{
\code{\link[=xreaduci]{xreaduci()}}, \code{\link[=xsaveuci]{xsaveuci()}}
}
