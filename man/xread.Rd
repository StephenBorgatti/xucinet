% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{xread}
\alias{xread}
\title{Read a network from a file}
\usage{
xread(
  file,
  filetype = NULL,
  layout = NULL,
  sheet = 1,
  labels = TRUE,
  directed = NULL,
  mode = NULL,
  title = NULL,
  ...
)
}
\arguments{
\item{file}{Path to the file. For UCINET datasets give either the \code{.##h} file
or the name without extension.}

\item{filetype}{One of \code{"ucinet"} (\code{.##h}/\code{.##d}), \code{"uci"} (JSON), \code{"dl"},
\code{"csv"}, \code{"xlsx"}, \code{"vna"}; \code{NULL} detects from the extension.}

\item{layout}{One of \code{"matrix"}, \code{"edgelist"}, \code{"nodelist"}; \code{NULL} detects
from the shape of the data (only relevant for csv/xlsx).}

\item{sheet}{Sheet name or number for \code{.xlsx} files.}

\item{labels}{Logical; does the first row/column hold node labels? (csv/xlsx)}

\item{directed, mode, title}{Passed to \code{\link[=as_xucinet]{as_xucinet()}}.}

\item{...}{Reserved.}
}
\value{
An \code{xucinet} object.
}
\description{
One importer for every file type the book uses. The \emph{file type} (the
container) is detected from the extension; the \emph{layout} (how the data are
arranged inside it) is detected from the shape of what is read. Both can be
overridden.
}
\section{Shipped datasets}{

If \code{file} names no file on disk but does name one of the datasets the package
ships, that dataset is used. So \code{xdensity("campnet")} works the way the
UCINET command line does, without a \code{data(campnet)} first. A real file always
wins over a dataset of the same name.
}

