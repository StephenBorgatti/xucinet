% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-xucinet.R
\name{[.xucinet}
\alias{[.xucinet}
\title{Subset the nodes of a network}
\usage{
\method{[}{xucinet}(x, i, j, ..., drop = FALSE)
}
\arguments{
\item{x}{An \code{xucinet} object.}

\item{i}{Rows (and, for a 1-mode network with \code{j} missing, columns) to keep.}

\item{j}{Columns to keep.}

\item{...}{Unused.}

\item{drop}{If \code{TRUE}, return the plain matrix (or, for a multi-relation
dataset, the named list of matrices) instead of an \code{xucinet} object.
Defaults to \code{FALSE}, unlike matrix subsetting.}
}
\value{
An \code{xucinet} object, or a matrix if \code{drop = TRUE}.
}
\description{
\code{net[i]} keeps nodes \code{i} of a 1-mode network, taking the same rows and
columns so the result stays square. \code{net[i, j]} keeps rows \code{i} and columns
\code{j}, which is what 2-mode data needs. Indices may be positions, negative
positions to drop nodes, a logical vector, or node labels. Labels, mode,
directedness, the dataset title and every relation of a multi-relation stack
are carried through.
}
\examples{
m <- matrix(c(0,1,1, 1,0,0, 1,0,0), 3, 3,
            dimnames = list(c("a","b","c"), c("a","b","c")))
net <- as_xucinet(m)
net[c("a", "b")]
net[-1]
}
