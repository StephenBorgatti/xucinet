% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-ucinet.R
\name{xsaveucinet}
\alias{xsaveucinet}
\title{Write a UCINET dataset}
\usage{
xsaveucinet(
  net,
  file,
  version = 6404,
  datatype = "single",
  title = NULL,
  istable = FALSE
)
}
\arguments{
\item{net}{A network (any accepted form).}

\item{file}{Output path, with or without the \code{.##h} extension.}

\item{version}{Header version to write: 4010, 4020, 5000, 6000, 6404 or 6405.}

\item{datatype}{Element type for the \code{.##d} file. \code{"single"} is what UCINET
itself defaults to; \code{"double"} keeps full R precision but older UCINET
routines may not expect it.}

\item{title}{Dataset title stored in the header. Defaults to the network's.}

\item{istable}{Value of the trailing \code{istable} byte, version 6405 only.}
}
\value{
The path of the \code{.##h} file written, invisibly.
}
\description{
Writes the \code{.##h} / \code{.##d} pair that UCINET reads. The default header version
is 6404, the version every dataset shipped with the book uses; 6405 adds one
trailing byte and is what current UCINET writes by default.
}
\details{
\code{NA} is written as 1e38, which is the value UCINET reads back as missing.
}
\examples{
m <- matrix(c(0,1,1, 1,0,0, 1,0,0), 3, 3)
f <- file.path(tempdir(), "demo")
xsaveucinet(m, f)
xreaducinet(f)
}
