% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xrege.R
\name{xrege}
\alias{xrege}
\title{Regular equivalence (REGE)}
\usage{
xrege(net, iterations = 3, relations = NULL, geodesics = FALSE, k = NULL)
}
\arguments{
\item{net}{A network (any accepted form). 1-mode; no negative values.}

\item{iterations}{Number of rounds. UCINET's default is 3.}

\item{relations}{Which relations to use, by name or position. \code{NULL}, the
default, uses all of them, as UCINET does.}

\item{geodesics}{Replace each relation by its geodesic distances first
(dichotomized, unreachable pairs at \code{n})?}

\item{k}{Optional number of clusters: fills the \code{Cluster} column.}
}
\value{
An object of class \code{c("xrege", "xucinet_output")}. \verb{$matrices}
holds the REGE similarities on 0-1 (UCINET prints them times 100);
\verb{$nodes} the partitions of the clustering and \code{Cluster}; \verb{$hclust} the
tree.
}
\description{
UCINET: Network | Roles & Positions | Maximal Regular Equivalence | REGE.
White and Reitz's algorithm: two nodes are regularly equivalent when they
are tied to equivalent others, not necessarily the same others. The
similarities start at 1 and are refined for \code{iterations} rounds; each round
matches every tie of one node with the best-matching tie of the other,
weighting the match by how equivalent the two alters were in the round
before.
}
\details{
The similarity matrix is clustered as UCINET does, with its weighted-average
linkage (\code{\link[=xhclust]{xhclust()}}'s \code{"average"}). The book (12.7) clusters it by single
link instead: pass \verb{$matrices[[1]]} to \code{\link[=xhclust]{xhclust()}}.

UCINET's REGE takes square data only, so there is no 2-mode REGE.
}
\section{UCINET equivalent}{

Network | Roles & Positions | Maximal Regular Equivalence | REGE. \emph{Max # of
iterations} is \code{iterations}, \emph{Convert to geodesics} is \code{geodesics}.
}

\examples{
xrege(campnet)
}
\seealso{
\code{\link[=xstructuralequivalence]{xstructuralequivalence()}}, \code{\link[=xhclust]{xhclust()}}.
}
