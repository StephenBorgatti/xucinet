% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xmds.R
\name{xmds}
\alias{xmds}
\title{Multidimensional scaling}
\usage{
xmds(
  x,
  type,
  method = c("classical", "nonmetric"),
  dim = 2,
  plot = TRUE,
  labels = NULL
)
}
\arguments{
\item{x}{A square proximity matrix: a matrix, \code{dist} object, data frame,
\code{xucinet} object or file name. An asymmetric matrix is symmetrized by
averaging and the report says so.}

\item{type}{\code{"similarities"} or \code{"dissimilarities"} (or \code{"s"} / \code{"d"}).
Required; see Details.}

\item{method}{\code{"classical"} or \code{"nonmetric"}.}

\item{dim}{Number of dimensions, at least 1 and less than the number of
objects.}

\item{plot}{Draw the map of the first two dimensions with base graphics.
With \code{dim = 1} a one-dimensional strip is drawn.}

\item{labels}{Optional character vector of point labels; defaults to the
row names of \code{x}.}
}
\value{
An \code{xucinet_output} of subclass \code{xmds}. \verb{$nodes} holds the
coordinates (\verb{Dim1 ... Dimk}, original object order). \verb{$summary} holds
\code{Stress} and, for the classical solution, \code{GOF1} and \code{GOF2}. For the
classical solution \verb{$matrices$Eigenvalues} holds each positive eigenvalue
and its proportion of the positive total, and \verb{$eig} the full eigenvalue
vector. For the non-metric solution \verb{$shepard} is a data frame with one
row per pair (\code{Dissimilarity}, \code{Distance}, \code{Disparity}), which
\code{\link[=xshepard]{xshepard()}} draws. \verb{$dissimilarities} is the matrix that was scaled,
after conversion.
}
\description{
UCINET: Tools | Scaling/Decomposition | Non-metric MDS (and Classic MDS).
Places the objects of a proximity matrix in \code{dim} dimensions so that the
distances between points reproduce the proximities: exactly as possible,
by classical (Torgerson) scaling, or in rank order, by Kruskal's non-metric
scaling. Book section 6.2.
}
\details{
\code{type} has no default. A matrix of 0/1 ties, a matrix of geodesic distances
and a matrix of co-membership counts all have the same shape, and only the
caller knows whether large values mean close or far. Pass \code{type = "similarities"} when larger values mean closer (correlations, tie strengths,
co-memberships) and \code{type = "dissimilarities"} when larger values mean
further apart (distances, geodesics). Similarities are converted to
dissimilarities as \code{max - x}, where the maximum is taken over the
off-diagonal cells.

\code{method = "classical"} is \code{stats::cmdscale()} on the dissimilarities.
\code{method = "nonmetric"} is \code{MASS::isoMDS()}, Kruskal's algorithm with
monotone regression, started from the classical solution and stopped after
100 iterations or when stress improves by less than 0.001. Because the
start is the classical solution there is no randomness, and the result is
reproducible without a seed.

Stress is Kruskal's stress formula 1, \code{sqrt(sum((d - dhat)^2) / sum(d^2))},
on a 0-1 scale. For the classical solution \code{dhat} is the raw dissimilarity;
for the non-metric solution it is the monotone-regression disparity. UCINET
reports its metric stress with the configuration distances in the
denominator, so the two agree only at perfect fit (differences vignette).
\code{GOF1} and \code{GOF2} for the classical solution are the shares of the summed
absolute eigenvalues and of the summed squared positive eigenvalues carried
by the first \code{dim} dimensions.

An MDS configuration is determined only up to rotation, reflection and
translation. A map that looks rotated or mirror-imaged against UCINET's, or
against a map drawn from a different start, is the same map.

UCINET's \emph{Metric MDS} (iterative stress minimization on the raw
proximities) has no counterpart here; see the differences vignette for why.
}
\examples{
xmds(cities, type = "dissimilarities", plot = FALSE)
xmds(cities, type = "d", method = "nonmetric", plot = FALSE)
}
\references{
Kruskal, J. B. (1964). Multidimensional scaling by optimizing
goodness of fit to a nonmetric hypothesis. \emph{Psychometrika}, 29, 1-27.
Torgerson, W. S. (1952). Multidimensional scaling: I. Theory and method.
\emph{Psychometrika}, 17, 401-419.
}
\seealso{
\code{\link[=xshepard]{xshepard()}}, \code{\link[=xcorrespondence]{xcorrespondence()}}, \code{\link[=xhclust]{xhclust()}}
}
