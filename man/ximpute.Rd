% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ximpute.R
\name{ximpute}
\alias{ximpute}
\title{Fill in missing ties}
\usage{
ximpute(
  net,
  method = c("remo", "re", "mo", "mean", "tm", "knn", "nti", "random", "copy"),
  ties = c("zero", "density", "random", "missing"),
  cutoff = 0.5,
  round = TRUE,
  k = 3,
  value = 0,
  seed = NULL
)
}
\arguments{
\item{net}{A network (any accepted form). 1-mode only.}

\item{method}{How to impute. \code{"remo"} (the dialog's default) reconstructs
from the transpose and then fills what is left with the modal incoming
tie; \code{"re"} reconstructs only; \code{"mo"} uses the modal incoming tie, for
binary data; \code{"mean"} the mean incoming tie, for valued data; \code{"tm"} the
global mean; \code{"knn"} the median among the \emph{k} nodes whose columns are
most like the missing node's; \code{"nti"} puts \code{value} everywhere;
\code{"random"} draws from the observed ties; \code{"copy"} uses what the node's
own contacts said.}

\item{ties}{For \code{"re"} and \code{"remo"}, what to do with a pair that is missing
both ways and so cannot be reconstructed: \code{"zero"} (the dialog's
default), \code{"density"} (1 everywhere if the density reaches \code{cutoff}, 0
otherwise), \code{"random"} (1 with probability equal to the density) or
\code{"missing"} to leave it.}

\item{cutoff}{The density threshold for \code{ties = "density"}.}

\item{round}{Round the imputed value to a whole number, for \code{"mean"} and
\code{"tm"}? Rounding is half-up, as the dialog's default is; UCINET's
banker's-rounding option is not offered.}

\item{k}{Neighbours for \code{"knn"}.}

\item{value}{The value \code{"nti"} fills with.}

\item{seed}{Seed for the methods that draw at random.}
}
\value{
An \code{xucinet} object titled \verb{<name>-imp}, with a \code{history} attribute
recording the method and how many cells were filled. The diagonal comes
back missing.
}
\description{
UCINET: Transform | Znidarsic et al Imputation of Ties. Replaces missing
cells using the rest of the network.
}
\details{
The diagonal is set missing first and is never imputed, as in UCINET.
}
\examples{
m <- matrix(c(0,1,1, NA,0,1, 0,NA,0), 3, 3, byrow = TRUE,
            dimnames = list(letters[1:3], letters[1:3]))
as.matrix(ximpute(m))
as.matrix(ximpute(m, method = "re"))
}
\seealso{
\code{\link[=xreplacemissing]{xreplacemissing()}} to fill from another dataset instead.
}
