% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xregression.R
\name{xregression}
\alias{xregression}
\title{Node-level regression with a permutation test}
\usage{
xregression(formula, data = NULL, nperm = 10000, seed = NULL, tails = 2)
}
\arguments{
\item{formula}{A model formula, \code{y ~ x1 + x2}.}

\item{data}{Where the variables are: a data frame, a node-level result (its
\verb{$nodes} is used), or a network with attributes. \code{NULL} looks the names
up where the formula was written.}

\item{nperm}{Number of random permutations. UCINET's default is 10000.}

\item{seed}{Random seed for the permutations.}

\item{tails}{2 (the default) or 1. A one-tailed test takes its direction
from the sign of the observed statistic.}
}
\value{
An object of class \code{c("xregression", "xucinet_output")}.
\verb{$matrices} has the coefficient table: \code{Coef}, \code{Beta}, \code{SE}, \code{T},
\code{c.Sig} (classical), \code{p.Sig} (permutation) and the three proportions
\verb{As Large}, \verb{As Small}, \verb{As Extreme}. \verb{$summary} has the number of
cases, R-square, adjusted R-square, F and its classical and permutation
significance. \verb{$nodes} has the predicted values and residuals.
}
\description{
UCINET: Tools | Testing Hypotheses | Node-level | Regression. Ordinary least
squares regression of one node attribute on others, with p-values from
permuting the dependent variable: the observed t of each coefficient is
compared with the t's obtained when the values of the dependent variable
are shuffled across the nodes. This asks whether the relationship could
have arisen by chance among these nodes, without assuming they are a
sample from a population (book, 14.3-14.4).
}
\details{
\code{nperm = 0} gives the classical test only.
}
\section{UCINET equivalent}{

Tools | Testing Hypotheses | Node-level | Regression. \emph{No. of random
permutations} is \code{nperm}, \emph{p-values} is \code{tails}, \emph{Method} Classical is
\code{nperm = 0}.
}

\examples{
d <- data.frame(y = c(1, 3, 2, 5, 4, 6), x = c(1, 2, 3, 4, 5, 7),
                row.names = letters[1:6])
xregression(y ~ x, d, nperm = 1000, seed = 1)
}
\seealso{
\code{\link[=xcorrelation]{xcorrelation()}}, \code{\link[=xmrqap]{xmrqap()}}.
}
