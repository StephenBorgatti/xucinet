% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xcliques.R
\name{xcliques}
\alias{xcliques}
\title{Cliques}
\usage{
xcliques(net, min = 3, type = c("weak", "strong"), relation = NULL)
}
\arguments{
\item{net}{A network (any accepted form). 1-mode.}

\item{min}{The smallest clique to report. UCINET's default is 3.}

\item{type}{\code{"weak"} (the default: a tie in either direction) or
\code{"strong"} (ties both ways).}

\item{relation}{Which relation of a multi-relation dataset, by name or
position. Defaults to the first.}
}
\value{
An object of class \code{c("xcliques", "xucinet_output")} with
\verb{$cliques}, a list of label vectors in UCINET's order;
\verb{$matrices$participation} (node by clique) and \verb{$matrices$comembership}
(node by node); and \verb{$clustering}, the \code{\link[=xhclust]{xhclust()}} result on the
co-membership matrix. \verb{$comembership} is the same matrix, for
\code{xhclust(xcliques(net)$comembership, type = "similarities")}.
}
\description{
UCINET: Network | Subgroups | Cliques. Every maximal complete subgraph of at
least \code{min} nodes, how much each node takes part in each, and a clustering
of the nodes by how many cliques they share.
}
\details{
A clique is a set of nodes all tied to one another that no other node could
join. Directed data are made undirected first: a tie in either direction
counts (UCINET's \emph{Weak} cliques, the default), or with \code{type = "strong"}
only ties that go both ways. Valued data are treated as binary.

The report lists the cliques in the order UCINET finds and numbers them,
then the participation scores (for each node and clique, the proportion of
the clique's members the node is tied to, counting itself), then the
hierarchical clustering of the co-membership matrix, which counts for each
pair of nodes the cliques they are both in. That matrix is kept in the object
but not printed, as in UCINET.
}
\section{UCINET equivalent}{

Network | Subgroups | Cliques. \emph{Minimum size} is \code{min}; \emph{Type} is \code{type}.
}

\examples{
xcliques(campnet)
}
\seealso{
\code{\link[=xfactions]{xfactions()}} and \code{\link[=xcommunities]{xcommunities()}} for partitions rather than
overlapping groups; \code{\link[=xhclust]{xhclust()}}.
}
