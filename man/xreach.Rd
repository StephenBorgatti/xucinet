% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xreach.R
\name{xreach}
\alias{xreach}
\title{Reach centrality}
\usage{
xreach(net, relation = NULL, directed = NULL, direction = c("out", "in"))
}
\arguments{
\item{net}{A network (any accepted form).}

\item{relation}{Which relation of a multi-relation dataset, by name or
position. Defaults to the first.}

\item{directed}{\code{NULL} (detect from symmetry), \code{TRUE} or \code{FALSE}. Directed
data is reported out-wards; use \code{direction = "in"} for the other.}

\item{direction}{\code{"out"} (default) or \code{"in"}. UCINET reports both for
directed data, in separate tables.}
}
\value{
An \code{xucinet_output}.
}
\description{
UCINET: Network | Centrality | Reach Centrality. How much of the network a
node can get to, and how quickly.
}
\details{
Three things are reported. \verb{$nodes} holds the two summary measures UCINET
calls \code{dwr} and \code{ard}: distance-weighted reach, the sum of the reciprocals of
a node's geodesic distances, and average reciprocal distance, the same figure
divided by \code{n - 1}. \verb{$matrices$ReachProp} holds the wide table -- the
proportion of the network a node reaches within one step, two steps, and so
on to \code{n - 1}.

Unreachable pairs contribute nothing, so a node in a small component simply
plateaus early: on a disconnected graph the row stops rising at the size of
its own component. That is the natural reading and it is what UCINET does.
}
\examples{
xreach(campnet)
}
