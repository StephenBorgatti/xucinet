% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xinverseweighteddegree.R
\name{xinverseweighteddegree}
\alias{xinverseweighteddegree}
\title{Inverse-weighted degree centrality}
\usage{
xinverseweighteddegree(net, relation = NULL, normalize = TRUE)
}
\arguments{
\item{net}{A network (any accepted form). 1-mode. Missing cells count as 0.}

\item{relation}{Which relation of a multi-relation dataset, by name or
position. Defaults to the first.}

\item{normalize}{Print the normalized scores first? \code{TRUE}, the default,
as UCINET's dialog.}
}
\value{
An object of class \code{c("xinverseweighteddegree", "xucinet_output")}
whose \verb{$nodes} holds \code{OutIWD}, \code{InIWD}, \code{nOutIWD} and \code{nInIWD}, or \code{IWD}
and \code{nIWD} for symmetric data.
}
\description{
UCINET: Network | Centrality | Inverse-Weighted Degree. Degree in which each
tie is weighted by the inverse of the other node's degree, so a tie to a
node with few ties counts for more than a tie to one with many.
}
\details{
Ported from \code{Tiwdcentrality.run} in \code{uc_iwdcentrality.pas} (repository
StephenBorgatti/ucinet at commit c7b4956). With row totals \code{r} and column
totals \code{c},

\describe{
\item{\code{OutIWD}}{\verb{sum_j x[i,j] / c[j]}: i's ties, each weighted by the
inverse of the alter's indegree.}
\item{\code{InIWD}}{\verb{sum_i x[i,j] / r[i]}: the column sums of the
row-stochastic matrix.}
}

For symmetric data the two are the same and only one is reported, as \code{IWD}.
The normalized scores divide by \code{n - 1}, the largest possible raw score,
since every term is at most 1. Both versions are always in \verb{$nodes};
\code{normalize} chooses which is printed first. UCINET's own output is the
normalized one.

Three things differ from UCINET 6.849, which has faults here (UCINET issue
28, ledger entry 33): the diagonal is always excluded, the totals are
computed for the relation analysed, and the normalized score is \code{raw/(n-1)}
for valued data as well as binary.
}
\examples{
xinverseweighteddegree(campnet)
}
\seealso{
\code{\link[=xdegree]{xdegree()}}.
}
