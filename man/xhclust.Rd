% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xhclust.R
\name{xhclust}
\alias{xhclust}
\title{Johnson's hierarchical clustering}
\usage{
xhclust(
  x,
  type,
  method = c("average", "single", "complete"),
  k = NULL,
  plot = TRUE
)
}
\arguments{
\item{x}{A square proximity matrix: a matrix, \code{dist} object, data frame,
\code{xucinet} object or file name. An asymmetric matrix is symmetrized by
averaging, as UCINET does, and the report says so.}

\item{type}{\code{"similarities"} or \code{"dissimilarities"} (or \code{"s"} / \code{"d"}).
Required; see Details.}

\item{method}{Linkage: \code{"average"} (UCINET's default, \code{WTD_AVERAGE}),
\code{"single"} or \code{"complete"}.}

\item{k}{Optional number of clusters. Fills the \code{Cluster} column of \verb{$nodes}
with \code{cutree(hc, k)}; the level table itself is unchanged.}

\item{plot}{Draw the dendrogram with base graphics. The text cluster diagram
is printed regardless.}
}
\value{
An \code{xucinet_output} of subclass \code{xhclust}. \verb{$nodes} is the
partition indicator matrix as a data frame, one column per distinct merge
level, named as UCINET names them (\verb{1(8)206} is level 1, eight clusters,
merge distance 206), plus \code{Cluster}, which is missing unless \code{k} is
given. As in UCINET, which saves this matrix but does not print it, it is
not part of the printed report. \verb{$summary} holds the cophenetic
correlation, the number of levels and the number of clusters requested
(missing without \code{k}). \verb{$matrices} holds
\verb{Measures of cluster adequacy} (\code{Corr}, \code{Modularity}, \code{Silhouette} by
level) and \verb{Cluster sizes} (proportion of items in each cluster, by
level). \verb{$hclust} is the underlying \code{stats::hclust} object, so \code{cutree()}
and \code{as.dendrogram()} work on it directly. \verb{$levels} is the numeric vector
of merge distances.

\code{Corr} is the correlation between the proximities and a same-cluster
indicator, signed so that higher is better for both input types; for
dissimilarity input UCINET prints it with the opposite sign. \code{Modularity}
is Newman's Q of the partition on the similarity matrix (for dissimilarity
input, on \code{1 / (1 + d)}). \code{Silhouette} is the average silhouette width on
the dissimilarities.
}
\description{
UCINET: Tools | Cluster Analysis | Johnson's Hierarchical. Agglomerative
clustering of a proximity matrix by single, complete or average linkage
(Johnson, 1967), reported as UCINET reports it: a text cluster diagram, the
partition at each merge level, measures of cluster adequacy and cluster
sizes. Book section 6.4.
}
\details{
\code{type} has no default. A matrix of 0/1 ties, a matrix of geodesic distances
and a matrix of co-membership counts all have the same shape, and only the
caller knows whether large values mean close or far. Pass \code{type = "similarities"} when larger values mean closer (correlations, tie strengths,
co-memberships) and \code{type = "dissimilarities"} when larger values mean
further apart (distances, geodesics). Similarities are converted to
dissimilarities as \code{max - x}, where the maximum is taken over the
off-diagonal cells.

UCINET records a new partition only when the merge distance changes, so
tied merges collapse into one level; the partition table here does the
same, which is why it can have fewer columns than \code{n - 1}. Where merge
distances tie, UCINET merges the first tied pair in original index order and
\code{hclust()} uses its own rule, so the two can produce different (equally
correct) merge orders at the same levels; see the differences vignette.

On linkage names: UCINET's \code{WTD_AVERAGE} is the size-weighted average
\code{(x * s1 + y * s2) / (s1 + s2)}, which is UPGMA and \code{hclust(method = "average")}. Its \code{SIMPLE_AVERAGE} is \code{(x + y) / 2}, which R calls
\code{"mcquitty"} and is not offered here. The adjectives run the opposite way
from R's, which is why this note exists.
}
\examples{
xhclust(cities, type = "dissimilarities", method = "single", plot = FALSE)

res <- xhclust(cities, type = "d", k = 3, plot = FALSE)
res$nodes$Cluster
}
\references{
Johnson, S. C. (1967). Hierarchical clustering schemes.
\emph{Psychometrika}, 32, 241-254.
}
\seealso{
\code{\link[=xmds]{xmds()}}, \code{\link[=xcorrespondence]{xcorrespondence()}}
}
