% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xdegree.R
\name{xdegree}
\alias{xdegree}
\title{Degree centrality}
\usage{
xdegree(
  net,
  relation = NULL,
  directed = NULL,
  weighted = NULL,
  normalize = FALSE,
  diagonal = FALSE
)
}
\arguments{
\item{net}{A network (any accepted form).}

\item{relation}{Which relation of a multi-relation dataset, by name or
position. Defaults to the first.}

\item{directed}{\code{NULL} (detect from symmetry, as UCINET's \emph{Auto-detect}),
\code{TRUE} or \code{FALSE}. \code{FALSE} symmetrizes by union first, as UCINET does.}

\item{weighted}{\code{NULL} or \code{TRUE} to use tie values, \code{FALSE} to dichotomize at
\verb{> 0} first. UCINET's \emph{Allow edge weights}, which defaults to on.}

\item{normalize}{Divide by the maximum possible degree. Both raw and
normalized columns are always returned; this selects which is reported as
the primary value by \code{\link[=summary]{summary()}} and by \verb{sort =}.}

\item{diagonal}{Include the diagonal? UCINET's \emph{Exclude diagonal} defaults to
on, so this defaults to \code{FALSE}.}
}
\value{
An \code{xucinet_output}. \verb{$nodes} holds the degree columns in original
node order; \verb{$summary} holds graph centralization, as a proportion rather
than a percentage, which is what UCINET reports.
}
\description{
UCINET: Network | Centrality | Degree. The number of ties each node has, or
the sum of their values when the data are valued.
}
\details{
Ported from \code{uc_DegreeCentrality.pas} rather than from a description, so the
column headings, the normalization denominator and the graph centralization
formula are UCINET's own. Directed data gives four columns, \code{Outdeg}, \code{Indeg},
\code{nOutdeg} and \code{nIndeg}; symmetric data gives \code{Degree} and \code{nDegree}. UCINET
can be made to print fewer by unticking boxes in its dialog; we always print
all of them (see the differences vignette).
}
\examples{
xdegree(campnet)

# UCINET's sorted view is a printing choice, not an argument of the routine:
print(xdegree(campnet), sort = "Indeg")
}
