% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xbeta.R
\name{xbeta}
\alias{xbeta}
\title{Beta centrality (Bonacich power)}
\usage{
xbeta(
  net,
  relation = NULL,
  beta = NULL,
  direction = c("in", "out"),
  normalize = TRUE
)
}
\arguments{
\item{net}{A network (any accepted form).}

\item{relation}{Which relation of a multi-relation dataset, by name or
position. Defaults to the first.}

\item{beta}{The attenuation parameter. \code{NULL}, the default, is UCINET's
automatic choice: \code{0.999} divided by the largest eigenvalue, which is just
inside the value where the series stops converging.}

\item{direction}{\code{"in"} (default, comparable to indegree, as UCINET) or
\code{"out"}.}

\item{normalize}{Rescale so the sum of squares equals \code{n}? UCINET's default,
and on by default here.}
}
\value{
An \code{xucinet_output} whose \verb{$nodes} has one column, named for the beta
used, as UCINET names it: \code{B0.10000}.
}
\description{
UCINET: Network | Centrality | Beta Centrality (Bonacich Power). Degree-like
when beta is zero, eigenvector-like as beta approaches its limit, and
something else when beta is negative: being connected to well-connected
others becomes a disadvantage, which is Bonacich's point about bargaining.
}
\details{
Ported from \code{runbetacent} in \code{Xdpmat.pas}. The score is
\code{(I - beta*A)^-1 * A * 1}, rescaled so that the sum of squares equals the
number of nodes, which is UCINET's \emph{ssq = n} normalization and its dialog
default.

UCINET computes \strong{in} centrality by default -- it transposes the matrix
first, so the score is comparable to indegree -- and says so in its log. That
is reproduced here rather than quietly changed.
}
\examples{
xbeta(campnet, beta = 0.1)
}
