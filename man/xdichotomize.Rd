% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform.R
\name{xdichotomize}
\alias{xdichotomize}
\title{Dichotomize a valued network}
\usage{
xdichotomize(
  net,
  cutoff = 0,
  op = c(">", ">=", "==", "<=", "<", "!="),
  then = 1,
  otherwise = 0,
  density = NULL,
  method = c("cutoff", "density", "maxcor"),
  diagonal = c("else", "zero", "missing", "then", "rule")
)
}
\arguments{
\item{net}{A network (any accepted form).}

\item{cutoff}{The value to compare against. Used when \code{method = "cutoff"},
which is the default.}

\item{op}{One of \code{">"} (UCINET's default), \code{">="}, \code{"=="}, \code{"<="}, \code{"<"} or
\code{"!="}.}

\item{then}{Value written where the rule holds. \code{1} by default; \code{NA} is
allowed, as in the dialog.}

\item{otherwise}{Value written where it does not. \code{0} by default; \code{NA} is
allowed. UCINET calls this the "else" value, which cannot be an argument
name in R because \verb{else} is reserved.}

\item{density}{Target density, used when \code{method = "density"}. The cutoff
chosen is the most selective one, among the values present, whose density
is still at least this.}

\item{method}{\code{"cutoff"} (default), \code{"density"} or \code{"maxcor"}.}

\item{diagonal}{What to write on the diagonal of a square result:
\code{"else"} (the dialog's default), \code{"zero"}, \code{"missing"}, \code{"then"}, or
\code{"rule"} to dichotomize it like any other cell. Ignored for 2-mode data,
where the diagonal is an ordinary cell.}
}
\value{
An \code{xucinet} object titled as UCINET names it (\code{camp92_GT_1}; a dot
in the cutoff becomes \code{p}), with a \code{history} attribute giving the cutoff
used and, for \code{"density"} and \code{"maxcor"}, the density and correlation it
achieved.
}
\description{
UCINET: Transform | Dichotomize. Applies the rule "if \code{x[i,j]} \emph{op} cutoff
then \code{then} else \code{otherwise}" to every cell.
}
\details{
The cutoff can be given three ways, chosen by \code{method}: as a value, as a
target density, or by maximising the correlation between the dichotomized
matrix and the original. The last is what UCINET's Transform | Dichotomize
Interactive does one row at a time - it tabulates every distinct value with
its correlation, number of ones and density, and the user picks a row - so
\code{method = "maxcor"} picks the best row automatically. Neither the density
target nor \code{maxcor} is a batch routine in UCINET; see the differences
ledger.
}
\examples{
m <- matrix(c(0,3,1, 2,0,0, 5,1,0), 3, 3, byrow = TRUE,
            dimnames = list(letters[1:3], letters[1:3]))
as.matrix(xdichotomize(m, cutoff = 1))
as.matrix(xdichotomize(m, cutoff = 1, op = "<="))

# the cutoff that best reproduces the valued matrix
d <- xdichotomize(m, method = "maxcor")
attr(d, "history")
}
\seealso{
\code{\link[=xsymmetrize]{xsymmetrize()}}, \code{\link[=xrecode]{xrecode()}}, \code{\link[=xdensity]{xdensity()}}.
}
