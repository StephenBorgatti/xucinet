% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xinduced.R
\name{xinduced}
\alias{xinduced}
\title{Induced centrality}
\usage{
xinduced(net, relation = NULL, k = 3)
}
\arguments{
\item{net}{A network (any accepted form).}

\item{relation}{Which relation of a multi-relation dataset, by name or
position. Defaults to the first.}

\item{k}{The cutoff for the "pairs within k links" column. UCINET's dialog
defaults to 3, and the column is named for whatever is used: \verb{W'in3}.}
}
\value{
An \code{xucinet_output} whose \verb{$nodes} holds the nine measures.
}
\description{
UCINET: Network | Centrality | Induced Centrality. How much of a
whole-network property a node is responsible for, measured by taking the node
out and seeing what changes.
}
\details{
Nine measures, each the difference in a whole-network statistic between the
network as it is and the network with this node's ties stripped away. The
unit behind the menu item is \code{uc_ContributionCentrality.pas}, and three of
its conventions are worth stating because none is the obvious choice:
\itemize{
\item \strong{The node is isolated, not deleted.} Its ties go; it stays in the graph.
So \code{n} is the same on both sides of every subtraction, and the distance
substitutions below mean the same thing on both sides.
\item \strong{Unreachable pairs count as \code{n}}, they are not skipped. That is what makes
removing a cut vertex \emph{raise} the distance total rather than lower it.
\item \strong{The comparison is deliberately asymmetric.} The intact figures are
computed once with every node counted; each stripped figure leaves the
stripped node out of the sums. Comparing like with like instead gives
different numbers, and they are not UCINET's.
}

\code{SumDist} and \code{Fragmentation} are reported the other way round from the rest,
since for those two an increase is the damage.
}
\examples{
xinduced(campnet)
}
