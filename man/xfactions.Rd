% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xfactions.R
\name{xfactions}
\alias{xfactions}
\title{Factions}
\usage{
xfactions(
  net,
  k = 2,
  method = c("hamming", "phi", "modularity", "entailment"),
  restarts = 3,
  maxit = 20,
  penalty = 15,
  seed = NULL,
  relation = NULL
)
}
\arguments{
\item{net}{A network (any accepted form). 1-mode. Valued data are
dichotomized.}

\item{k}{The number of factions. UCINET's default is 2.}

\item{method}{\code{"hamming"} (the default), \code{"phi"}, \code{"modularity"} or
\code{"entailment"}.}

\item{restarts}{Number of random starts; UCINET's default is 3.}

\item{maxit}{Steps the search runs past its last improvement; default 20.}

\item{penalty}{Steps a node is barred from returning to the group it left;
default 15.}

\item{seed}{The random seed, a whole number. \code{NULL} picks one between 1 and
1000, as the dialog does, and the report says which.}

\item{relation}{Which relation of a multi-relation dataset, by name or
position. Defaults to the first.}
}
\value{
An object of class \code{c("xfactions", "xucinet_output")}. \verb{$nodes}
has \code{Cluster}, the faction of each node; \verb{$summary} the fit under UCINET's
label, the number of factions and the modularity; \verb{$matrices} the block
densities.
}
\description{
UCINET: Network | Subgroups | Factions. Divides the nodes into \code{k} groups
so that ties fall inside groups and not between them, as nearly as
possible: the ideal is a network of \code{k} complete subgraphs with nothing
between them.
}
\details{
How near is measured by \code{method}:
\itemize{
\item \code{"hamming"} (the default): the number of cells that disagree with the
ideal, reported as the proportion that agree ("proportion correct").
\item \code{"phi"}: the correlation between the network and the ideal.
\item \code{"modularity"}: Newman's modularity.
\item \code{"entailment"}: the proportion of ties that stay inside a group.
}

The search is heuristic and starts from random partitions, so a different
\code{seed} can give a different answer on a network without clear factions. The
same seed gives the same answer, and the same answer as UCINET given the
seed UCINET's dialog shows.
}
\section{UCINET equivalent}{

Network | Subgroups | Factions. \emph{Number of blocks} is \code{k}, \emph{Measure of fit}
is \code{method}, \emph{Number of random starts} is \code{restarts}, \emph{Max # of iterations
in series} is \code{maxit}, \emph{Length of time in penalty box} is \code{penalty}, and
\emph{Random number seed} is \code{seed}.
}

\examples{
xfactions(campnet, k = 2, seed = 1)
}
\seealso{
\code{\link[=xcommunities]{xcommunities()}}, \code{\link[=xcliques]{xcliques()}}.
}
