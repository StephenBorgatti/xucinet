% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xeigenvector.R
\name{xeigenvector}
\alias{xeigenvector}
\title{Eigenvector centrality}
\usage{
xeigenvector(net, relation = NULL)
}
\arguments{
\item{net}{A network (any accepted form).}

\item{relation}{Which relation of a multi-relation dataset, by name or
position. Defaults to the first.}
}
\value{
An \code{xucinet_output}. \verb{$nodes} has one \code{Eigenvector} column;
\verb{$summary} carries the principal eigenvalue, or the component count when
the lambda-squared method was used instead.
}
\description{
UCINET: Network | Centrality | Eigenvector Centrality. Importance defined
recursively: a node is central to the extent that its neighbours are.
}
\details{
Ported from \code{runeigencent} in \code{Xdpmat.pas}. The data are symmetrized by
maximum first, as UCINET does, and it says so in the printed report.

On a \strong{connected} graph this is the principal eigenvector, scaled to unit
length and with its sign flipped positive; the principal eigenvalue is
reported beside it.

On a \strong{disconnected} graph UCINET switches method, and so do we. A principal
eigenvector is only defined up to a component, so running one on a
disconnected graph gives everything to a single component and zero to the
rest, which is arithmetic rather than a finding. UCINET falls back to the
Everett-Borgatti lambda-squared score, \code{lambda^2 * sum(v) * v(i)} computed
within each component, which puts the components on a common footing. The
switch happens when more than one component has more than one node, so
isolates alone do not trigger it.
}
\examples{
xeigenvector(campnet)
}
