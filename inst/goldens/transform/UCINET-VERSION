UCINET: n/a
Generated: not yet
Notes: Chapter 5 goldens (the data-management and transformation routines:
    symmetrize, dichotomize, normalize, geodesic distances, similarities,
    attribute to matrix, collapse). The batch has not been run. Until it is,
    every test that calls skip_if_no_golden(<name>, "transform") skips, and
    this family is exempt from the reference-build check. When the batch is
    run, set UCINET: to the build that produced the fixtures. See README.md
    here, and make_goldens.txt for the batch itself.
