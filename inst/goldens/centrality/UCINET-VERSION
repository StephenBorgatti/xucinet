UCINET: 6.849
Generated: 2026-09-07
Script: make_goldens.txt, regen_eigenvec.txt, make_goldens_2.txt
Notes: Three runs, all on 6.849. The eigenvec() fixtures were regenerated after
    UCINET issue 3 was fixed; the fix shipped inside 6.849 rather than under a
    new build number, so the version string here cannot distinguish the two
    binaries (UCINET request 10).
    .
    Two files, g9m_reach_disc and g9m_breach_disc, were re-encoded from single
    to double precision after UCINET wrote them. R CMD check flagged them as
    executable files: both begin with the bytes 8c 2e ba 3e, which is
    0.36363637 stored as a single, and something in that pattern trips its
    detector. The re-encode is lossless and the values are unchanged - the test
    suite compares them against the same numbers either way - but the bytes are
    no longer exactly what UCINET wrote, which is worth knowing if anyone ever
    diffs them against a fresh run.
    .
    Menu-run reports are the log_*.txt files here, one per procedure, converted
    from UCINET's UTF-16 to UTF-8. UCINET writes them to
    %AppData%/Roaming/AnalyticTech/Ucinet/ucinetlog<N>.txt, numbered by an
    internal counter with no relation to the routine; each was identified by its
    title line and renamed.
